# Devlog - 2026-08-11

## Recorder polish round: drawer, corners, meter energy - (morning)

**Status**: ✅ All shipped OTA, user signed off ("very good")

Iteration loop from yesterday paying off — five updates published, zero builds:

- White strip under the Completed tab: `SafeAreaView edges={["bottom"]}` was double-compensating (lists already pad `120 + insets.bottom`). Now `edges={[]}`, full-bleed under the home indicator. `app/index.tsx:1036`
- Drawer entrance (subagent): card rises from the bottom edge while stretching to full height — animated `height` + content bottom-anchored at fixed `openHeight` inside `overflow: hidden`, so the growth reveals content with zero reflow. First cut felt like a pop; retuned to ~450ms (damping 23, stiffness 160, mass 1).
- Concentric corners: new `lib/screenCorners.ts` — per-device display-radius table (no public iOS API), card radius = screen radius − gap, equal 10pt gap left/right/bottom. iPhone 12 lands at ≈37pt. Home-button devices fall back to 28.
- Voice meter energy: dB window widened to −52..−12 with a pow(0.6) perceptual lift, and split springs — fast attack (stiffness 500) / slow release (180) — so bars snap per syllable like a VU meter. `components/VoiceMeter.tsx`
- Fixed duplicated Android permissions in `app.json` (update:configure's JSON rewrite tripled the array).

Known cosmetic debt: the drawer's `overflow: hidden` clips the card's iOS shadow. Wrapper-view fix if it bothers anyone.

### Next Session

1. CL-4 decision (voice-over-chime vs stop-and-replace) → issue doc → implement observer
2. Apple Feedback on attended-presentation sound (bundled-wav A/B first)
3. Device test plan steps 1-18 (AlarmKit + cadence ladder) — the ship gate
4. UI redesign pass on reference screenshots (header title, screen access, input, list items)
5. Drawer card iOS shadow (clipped by overflow) — wrapper view if wanted
6. Carry-overs: RevenueCat log noise, OpenRouter top-up, dark status-bar icons, ElevenLabs paid tier
