# Devlog - 2026-08-11

## Recorder polish round: drawer, corners, meter energy - (morning)

**Status**: ✅ All shipped OTA, user signed off ("very good")

Iteration loop from yesterday paying off — five updates published, zero builds:

- White strip under the Completed tab: `SafeAreaView edges={["bottom"]}` was double-compensating (lists already pad `120 + insets.bottom`). Now `edges={[]}`, full-bleed under the home indicator. `app/index.tsx:1036`
- Drawer entrance (subagent): card rises from the bottom edge while stretching to full height — animated `height` + content bottom-anchored at fixed `openHeight` inside `overflow: hidden`, so the growth reveals content with zero reflow. First cut felt like a pop; retuned to ~450ms (damping 23, stiffness 160, mass 1).
- Concentric corners: new `lib/screenCorners.ts` — per-device display-radius table (no public iOS API), card radius = screen radius − gap, equal 10pt gap left/right/bottom. iPhone 12 lands at ≈37pt. Home-button devices fall back to 28.
- Voice meter energy: dB window widened to −52..−12 with a pow(0.6) perceptual lift, and split springs — fast attack (stiffness 500) / slow release (180) — so bars snap per syllable like a VU meter. `components/VoiceMeter.tsx`
- Fixed duplicated Android permissions in `app.json` (update:configure's JSON rewrite tripled the array).

Known cosmetic debt: the drawer's `overflow: hidden` clips the card's iOS shadow. Wrapper-view fix if it bothers anyone.

## Continued — UI redesign shipped, alarm escalation truth found - (evening)

**Status**: ✅ Redesign live OTA · ⚠️ escalation needs the locked-phone test + a native pass

Grilled the redesign into 13 locked decisions (`docs/ui-redesign.md`), filed OLD-54..59, then a
7-agent workflow built it in ~15 min: three pages (Today / Days / Settings), Tiimo dock on all of
them with the mic beside it, GPT-picked emoji chips on serif Fraunces cards, week-strip day pager
that resets to today on re-entry, PlanJoy settings, Tiimo edit sheet. Completed page and
`app/history.tsx` died — completed items live in a collapsed COMPLETE section per day. Fraunces
ships as a JS asset so it's OTA-safe (`expo install` tried to sneak an `expo-font` plugin into
app.json — reverted; watch for it on future installs). Convex deployed once (emoji field), then
five OTA updates through the day as device feedback landed: dock centered/lowered with thinner
strokes, empty Today fully silent, ⋯ menu + all multi-select machinery stripped, settings cut to
one notifications entry + Terms of Use row (rationale in plan.md "Settings Design"), diagnostics
page rebuilt in the app's card language — safe-area fixed, real AlarmKit alarms listed instead of
the always-zero notifee count.

Alarm escalation was a two-act story. Act 1: hydration race — variant wavs arrive after the
ladder registers, and the auto-opened edit sheet's save wiped them; fixed with execution-time
store reads (`withStoredWavUrls`) + refresh guards, 165 tests green. Act 2: user still heard one
line repeating — that's rung 0 looping its 28s silence-padded wav; iOS 26 loops one baked sound
until slide-to-stop and NO API auto-stops an unattended alert. Full research with citations in
`docs/cl4-escalation-research.md`: unattended escalation = sibling ladder only (whether a new
rung's audio replaces or mixes with the old loop is undocumented — needs the device test); true
stop-and-replace = native `alarmUpdates` + `stop(id:)` pass, which also fixes the attended
default-tone bug and the dead missed-path (native never logs "fired").

### Next Session

1. Locked-phone overlap test (procedure: cl4-escalation-research.md §4) → tune ladder offsets/rungs OTA
2. CL-4 native pass: alarmUpdates observer + stop(id:), native fired event, then full rebuild
3. Verify emoji chip on a fresh recording + edit sheet rows on device (closes OLD-58/59)
4. Device test plan steps 1-18 (AlarmKit + cadence ladder) — still the ship gate
5. Apple Feedback on attended-presentation sound (bundled-wav A/B first)
6. Host a privacy policy (App Store needs it; Terms row shipped today)
7. Carry-overs: RevenueCat log noise, OpenRouter top-up, dark status-bar icons, ElevenLabs paid tier, drawer card shadow
