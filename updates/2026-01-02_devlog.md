# Devlog 2026-01-02

## Fixed double-tap recording crash - (11:50)

**Status**: ✅ Fixed

Recording button would crash with "Only one Recording object can be prepared at a given time" when tapped twice quickly. Added guard to prevent race condition.

- `lib/audio.ts:5` - added `isRecordingPreparing` flag
- `lib/audio.ts:17-45` - wrapped `startRecording()` with guard and cleanup

---

## Fixed timezone bug for relative time parsing - (12:10)

**Status**: ✅ Fixed

"Remind me in 2 minutes" was being scheduled for 8am instead of current time + 2 minutes.

**Root cause**: Device was sending UTC time (`2026-01-02T08:07:26.431Z`) but user is in UTC+4, so 12:07 local was parsed as 08:07.

**Fix**: 
1. App now sends local date and time separately (no UTC conversion)
2. Added relative time rules to GPT prompt so it knows how to calculate "in X minutes"
3. Added detailed logging to trace the full pipeline

Files:
- `app/index.tsx:162-175` - sends `deviceLocalDate` and `deviceLocalTime` instead of ISO timestamp
- `convex/actions.ts:207-244` - uses local time directly, added logging
- `convex/actions.ts:268-272` - added "IMPORTANT - Relative time rules" to GPT prompt

---

## Removed noisy toasts + audio preloading - (14:58)

**Status**: ✅ Done

**Toasts**: Removed for individual mark-done and delete actions. Kept for bulk actions.

**Audio preloading**: Edit page now preloads audio when opening so playback is instant.

Files:
- `app/index.tsx:291,325` - removed toast calls for individual actions
- `app/reminder/edit.tsx:147-157` - preload audio on page load
- `app/reminder/edit.tsx:238-280` - reuse preloaded sound instead of loading on-demand

---

## Optimized back-to-back audio repeats - (15:00)

**Status**: ✅ Done

Sound was loading fresh for each repeat, causing lag. Now preloads once and replays with 50ms gap.

- `lib/audio.ts:125-180` - new `playAudioRepeated()` function
- `lib/notifications.ts:191` - uses new optimized function

---

## Alarm behavior improvements - (15:09)

**Status**: ✅ Done

1. **Default changed to "until_stopped"** - Reminders now play 30 times by default (~3-5 min)
2. **Missed state tracking** - Reminders are marked "missed" after audio finishes
3. **UI indicator** - Missed reminders show orange clock icon + "Missed" label

Files:
- `lib/storage.ts:41-42,60-61` - default `soundRepeatMode: "until_stopped"`
- `lib/notifications.ts:206-211` - safety cap 30, default until_stopped
- `lib/notifications.ts:217-228` - record missed after audio finishes
- `app/index.tsx:612-645` - visual indicator for missed vs completed
- `app/index.tsx:199-209` - new reminders default to until_stopped

---

## Full-screen alarm UI (Phase 3) - (16:20)

**Status**: ⏸️ Backlogged

Created initial implementation but requires additional native Android setup to work properly.

Files created:
- `plugins/withFullScreenAlarm.js` - config plugin for USE_FULL_SCREEN_INTENT permission
- `app/alarm.tsx` - full-screen alarm UI component (not active yet)

Reverted fullScreenAction from notifications - needs more native integration work.

---

## Audio caching + delete flow fix - (17:00)

**Status**: ✅ Done

**Problem**: When editing a reminder with no internet, reschedule would fail because:
1. `cancelReminder` was deleting the local audio file
2. `scheduleReminder` tried to re-download from Convex (network fail)

**Fix**:
1. Audio is now cached - skip download if file already exists locally
2. Separated `cancelReminder` (for rescheduling) from `deleteReminderWithAudio` (for full deletion)

Files:
- `lib/notifications.ts:36-57` - check if audio exists before downloading
- `lib/notifications.ts:162-183` - new `deleteReminderWithAudio()` function
- `app/index.tsx:34,314,384` - use `deleteReminderWithAudio` for actual deletions
- `app/reminder/edit.tsx:25,483` - use `deleteReminderWithAudio` for delete action
