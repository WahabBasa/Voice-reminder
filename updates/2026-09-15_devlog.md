# Devlog - 2026-09-15

## Claude vs Codex: cutting the 2.1 review video - (10:08)

**Status**: ✅ Same cut from both; Codex 2x faster on wall-clock, but its cheap-worker setup was silently broken

User filmed the App Review demo with a second phone (`LANDrop\1000079342.mp4`, 9m21s, 1920x1080 with
a -90° rotation tag → portrait). Task for both sides, same brief, same tools (`C:\Dev\video-toolkit`):
end the video 1 s after the app shows Pro state (top-right "Get Pro" pill gone + "Pro Activated" toast).
Codex = gpt-6-astra @ medium orchestrating `luna-worker` subagents; Claude = Opus 4.8 orchestrator with
opus-builder workers. Run simultaneously, output per side in `LANDrop\claude\` / `LANDrop\codex\`.

- Both pinned the same frame: first Pro frame 227.733 s, cut 228.733 s (frame 6862).
- Codex 21m22s / 147k tokens. Claude 41m06s / ~600k tokens — the orchestrator ended its turn three
  times waiting on workers (I had to resume it by hand each time) and picked `preset slow` (13 min
  encode alone). Its map worker also guessed the purchase at 07:42/09:04; the orchestrator overruled it
  from the contact sheets.
- Found afterwards: `~/.codex/agents/luna-worker.toml` had no `name` field, so Codex logged
  `Ignoring malformed agent role definition` and its "luna" workers almost certainly ran on astra. Fixed
  the file; the cost comparison for rounds 1–2 is void. Memory `codex-agent-role-name-field`.

**Round 2** (both): ramp the wait between "reminder set" and "alarm fires" to under 30 s. Claude:
A=53.75 s, B=183.50 s, 6x muted, done in 16m26s once told to block on workers and use `preset fast`.
Codex found A=53.65/B=183.40 then died on the ChatGPT plan usage limit — forfeit.

**Round 3** (Claude only): 15x (8.65 s in output) plus a burned-in caption "Sped up until the alarm
rings" during the ramp. Final: `LANDrop\claude\round3\final.mp4`, 1:47.7. drawtext refuses Windows
drive-letter font paths — copy the ttf next to the spec and reference it bare.

## Trying to run the YouTube upload dialog hands-off - (13:45)

**Status**: ❌ Not possible today; user clicked through it himself

Codex computer-use needs the screen and mouse (not background), and its screenshot capture failed on
the Brave window anyway. Codex confirmed it has no verified way to attach to a signed-in Brave. The
real background route is CDP: Brave relaunched with `--remote-debugging-port=9222` + a DOM driver
(`scripts/brave-cdp.py`). Two blockers: killing Brave is classifier-denied, and a plain second launch
just hands off to the running instance — the user has to Exit from the tray first. Never got there.

Uploaded unlisted: `https://www.youtube.com/watch?v=BT_JVbB8O6o`. YouTube auto-classifies it as a Short
(portrait, <3 min); the watch URL plays normally.

## 2.1 resubmitted - (16-09 07:41)

**Status**: ✅ Submission 373e825f, build 1.0 (6), Waiting for Review

The 08-19 submission was still open as "Unresolved Issues", not an info hold — a reply alone would
not have resumed review. Order that worked in ASC:

1. Version page → Build → swap 1.0 (2) for 1.0 (6) → Save (status flips to Prepare for Submission).
2. App Privacy → add **Customer Support** (App Functionality, linked via device ID — feedback rows
   carry `deviceId`; Apple counts "via device" as linked). Device ID / Other User Content / Crash Data
   were already declared.
3. App Review Information → Notes has a hard 4,000-char cap. Condensed the 7-item reply to 1,615 chars
   in `docs/asc-notes-4000.txt` (user wanted it terse); full reasoning stays in `docs/asc-review-notes.md`.
4. App Review page → submission → reply in Messages → Resubmit stays grey until you press **Update
   Review** on the version page → item flips to Ready for Review → Resubmit.

Six minutes after resubmit, two feedback reports arrived from iOS 27.0 / America/Los_Angeles ("The
first", "The only", kind=settings, build 6) — the reviewer poking the feedback form. Replied via
`npx convex run feedback:setStatus` (status `looking` + founder note) so the reply shows on their
device. Note: `npx.cmd convex run` breaks under Git Bash on the `C:\Program Files` path; plain `npx` works.

Files:
- `docs/asc-review-notes.md` - link, build 6, on-camera order, resubmit steps
- `docs/asc-notes-4000.txt` - new, the ASC Notes text
- `scripts/brave-cdp.py` - new, CDP tab driver (unused yet)
- `outputs/codex/2026-09-15-video-*.md`, `2026-09-15-yt-upload-*.md`, `2026-09-15-browser-bg-question*.md`

### Next Session
1. Watch the review verdict (submission 373e825f). If approved: OLD-126 Convex prod switch before any
   public release.
2. Feedback needs a founder UI + a way for Claude to reply from chat (today it's `convex run` by hand).
3. Ring-state anomaly from 09-12 07:05 (banner X marked done, `events=0`) — relaunch with the Wi-Fi
   syslog rolling, then the rest of the device matrix.
4. Feedback header icon + one-time tip (parked, Codex-agreed).
5. Engine A/B (dictation vs transcriber), Arabic on-device, parse latency.
6. Codex: MCP server with key `ak_…mXj4` returns 401 on every start (config.toml) — fix or drop.
