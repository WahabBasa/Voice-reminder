# Devlog - 2026-08-22

## Rejection formalized: same 2.1, zero new findings - (morning)

**Status**: ⚠️ resubmit pending new recording

ASC flipped build 2 to Rejected — the message is word-for-word the 08-20 info
request, formalized because no reply went out. Attached subscriptions frozen with
the version; they thaw on approval. User decision: re-record WITH mic audio
rather than ship the silent-mic edit. Everything else is staged — reply text in
`docs/asc-review-notes.md`, edit pipeline re-runs cheaply on take 2.

## Script project parked with locked formula - (day)

8 iterations distilled into "THE LOCKED ANATOMY" at the top of
`docs/content/fern-script-playbook.md`: world first → priced cage → inevitability
inversion → viewer's click → study montage with receipts → the failure that hands
the baton; one causal link per beat; test by TTS listen, never by reading.
Approved act-one opening marked ✅ in the script file with a TO WRITE map;
acts two/three flagged for full rewrite to the standard. Taste memory updated
with the user's tells ("things just happen" / "can't decipher" / "too much
explaining").

### Next Session

1. Re-record the demo WITH mic: Control Center → long-press Screen Record →
   Microphone ON (red) → Start. Fresh TestFlight install first so consent +
   permission prompts appear on camera. Flow: launch → consent → mic/notif
   prompts → speak a reminder (audible now) → let it ring unlocked → paywall →
   purchase (if already subscribed in sandbox, demo Restore Purchases instead —
   Apple accepts either).
2. Edit take 2 through video-toolkit, upload (unlisted YouTube/Drive), link into
   `docs/asc-review-notes.md` item 1, reply in Resolution Center AND paste into
   App Review Information → Notes, re-attach build 2 if detached, resubmit.
   Reply without resubmitting does nothing — both.
3. On approval, before pressing Release: confirm both subs + the 7-day intro on
   vr_pro_annual got approved with the version.
4. OLD-116: wrap video-toolkit as a skill + acceptance run (<10 min, <50k tokens).
5. Carry: shot-1 reshoot (kills the VoiceReminder label), Arabic (Saudi) listing,
   RC cleanup + In Review closeout (~34 issues), Anthropic key for point.py,
   OLD-108 variant-audio remnants, OLD-109 app-language selection.
