# Devlog - 2026-08-15

## The big rework: escalation dead, catches dead, schedules real - (05:48-10:30)

**Status**: ✅ on main + Convex dev · ✅ 952/952, tsc clean · ⚠️ device pass pending

- Grill session locked 12 decisions. Headlines: escalation ladder and catches removed; voice is one
  direct sentence ("Take your pills right now" canon, EN+AR); dismiss = snooze — identical audio back
  +5 min, 3 comebacks, hardcoded; schedule becomes a days×times grid (multi-time, intervals with an
  8:00-22:00 default window, every-N-days, specific date — "Thursday 8 and 9" is ONE reminder now);
  interval mode is premium; full grid editable in the sheet; typed composer rides the same parse;
  paywall goes Tiimo-pattern in our blue, proof slots flagged off, pain-point copy.
- Filed OLD-95..102, cancelled OLD-65 + OLD-53 (moot without escalation), rescoped OLD-64 — still the
  ship gate. Plan lives at ~/.claude/plans/jazzy-nibbling-hopper.md.
- 10-agent Opus workflow implemented all eight issues in ~90 min. Then AlarmKit research
  (docs/alarmkit-focus-breakthrough.md): Alarmpop is just AlarmKit — no entitlement, opt-in only.
  The load-bearing fact: NO app code runs when a ring times out unattended.
- Audit against that found the nag was dead on a locked phone (comebacks only fired on foreground
  reconcile), native snooze was uncapped, and alarms could vanish after OS updates (FB21273655).
  All fixed: comebacks pre-scheduled as +5/+10/+15 siblings cancelled-on-answer, snooze capped at 3,
  launch re-registration. Dead settings rows (addressTerm, snooze duration) deleted end to end.
- Convex pushed (speechCatchState gone, processTypedReminder live). Commit f973e9d.

## Build saga, then the composer entry moved - (10:30-11:30)

**Status**: ✅ iOS dev build installable · ✅ OTA on development branch

- EAS iOS build 1 failed: the `#available(iOS 26.1)` alert-init fork needs the 26.1 SDK, default
  image compiles 26.0 — an availability fork still compiles both branches. Build 2 on image
  "latest" died even earlier (Install dependencies). Fix: collapse to the deprecated stopButton
  init (identical runtime behavior on 26.1+, system stop control either way), default image → green.
  Commit 45b3b3e. Also: first successful `eas build` from an agent session — same
  powershell -NoProfile recipe as updates.
- Two OTAs to the development branch (rework, then the + entry). Exit code 1 from these commands is
  PowerShell stderr noise — read the log tail, "✔ Published!"/"✔ Build finished" is the truth.
- User swapped the composer entry: keyboard button beside the mic is gone; a Tiimo-style "+" sits
  top-right in the Today header instead. `app/index.tsx` (headerActions + addButton style),
  `components/BottomBar.tsx` (onCompose removed), `components/ComposerSheet.tsx` comment. 952/952.

### Next Session
1. OLD-64 device pass — the 20-minute locked-phone nag test is the whole ballgame. Checklist on the
   issue. Install link: expo.dev build a70e7ac1.
2. Stop-button semantics: built as Stop = done (chain dies), Later = snooze. User hasn't overridden;
   strict "only in-app done stops it" is a one-intent change if wanted.
3. Live evals for the new prompt contract (phrasing canon, one-reminder-multi-time takes).
4. Residual cleanup: app/alarm.tsx stale debug route, DatePickerModal dead half, typedTake missing
   from coverage config.
5. Paywall + composer design iteration when more references land; RevenueCat/ASC queue unchanged.

## Continued: icon shipped, RevenueCat iOS live, paywall detail pass, ASC paperwork - (13:20)

**Status**: ✅ commit 7fa1961 + OTA + build 48728676 done · 🔨 ASC subs mid-flow · 955/955

- **Icon (OLD-71 done)**: user picked mic-with-a-face on flat blue, generated in ChatGPT (Midjourney
  died at the subscription gate — no Discord path for agents either, bots can't invoke /imagine).
  Rebuilt by hand as SVG master (`assets/branding/icon-mic-face.svg`), rendered full-bleed 1024
  `assets/icon.png`, alpha stripped. Full-bleed fixes the black-corner worry — iOS masks corners itself.
- **RevenueCat iOS (OLD-72)**: created the App Store app in RC via MCP (app7f9e2d4095), wired the
  appl_ key into `lib/purchases.ts`, products `vr_pro_monthly`/`vr_pro_annual` attached to entitlement
  and offering (had to create the $rc_annual package — didn't exist). Real bug found: code checked
  `entitlements.active['pro']` but RC's lookup_key is `Pro` — purchases would NEVER have unlocked, any
  platform. `PRO_ENTITLEMENT_ID='Pro'` now, exported and used everywhere. Blank paywall explained:
  no iOS key ever existed, SDK skipped configure by design.
- **Paywall detail pass (OLD-102)**: Opus agent, user's Tiimo screenshots as spec. All-black text
  with weight hierarchy (gray tokens deleted), hairline row separators in FeatureTable, bold price
  segment in sticky caption + borderless footer, Fraunces 400/500 on plan names/prices, hero
  moon+dots removed, 2px ink selected-card border. 955 tests. Shipped in the OTA.
- **Shipped**: commit 7fa1961 → OTA to development (update 15d7a762) + EAS iOS dev build 48728676
  (icon rides it; dev build a70e7ac1 is superseded — install the new one for OLD-64).
- **ASC walkthrough (live, screen-by-screen)**: DSA = non-trader/no EU (no public address). Paid Apps
  Agreement signed; ENBD bank added (IBAN account digits = 4 zeros + account no; branch routing via
  bank chat — Al Barsha 2 wasn't named in Apple's list); W-8BEN submitted (no TIN, no treaty, all
  certs ticked). App record created: **"Remi: Voice Reminders"**, SKU voicereminder-ios, © Oldowan
  Tech. Reviewer notes drafted (descriptive tone, user rejected the numbered-briefing version).
  Subtitle planned: "The talking alarm clock" (split keyword families across name/subtitle).
- ⚠️ `convex/actions.ts` has an uncommitted user-made model swap (parse → openai/gpt-5.6-luna,
  reasoning_effort none). Not mine, not deployed, left unstaged.

### Next Session
1. ASC finish line: Monetization → Subscriptions → group `Pro`, products `vr_pro_monthly` (1mo) +
   `vr_pro_annual` (1yr + 7-day intro trial), prices → Ready to Submit. Then Users and Access →
   Integrations → In-App Purchase key → .p8 into RevenueCat. Paywall fills itself after.
2. OLD-64 device pass on build 48728676 — still the ship gate. Also eyeball: new icon, paywall
   detail pass, MOST POPULAR pill clipping (agent flagged Android overflow risk).
3. ASO session: user's videos → I extract → keywords/description/promo text; fresh iOS screenshots.
4. Remi rebrand pass when user confirms: home-screen display name, PRO_PRODUCT_NAME, paywall copy.
5. Production build + eas submit once subs + screenshots exist (first IAP reviews with the version).
6. Decide the convex/actions.ts luna swap: commit + convex push, or revert.
7. Carry: stop-button semantics, live evals, residual cleanup (alarm.tsx, DatePickerModal, typedTake).
