# Devlog - 2026-02-14

## Investigated recurring Android startup crash - (16:19)

**Status**: ?? Intermittent crash still present (`PlatformConstants`), not yet root-caused

Saw the crash again while opening the dev build from Expo Dev Client. Latest hit was at **2026-02-14 16:18:42** with:
- `TurboModuleRegistry.getEnforcing(...): 'PlatformConstants' could not be found`
- `AppRegistryBinding::startSurface failed. Global was not installed`

What I ran and what happened:
- Re-ran startup flow and local run flow (`git pull`, date check, Metro, `adb reverse`, `expo run:android --no-bundler`).
- Confirmed this is **not** from `expo-notifications`; app uses `@notifee/react-native` + custom alarm native modules.
- Reproduced and captured logcat traces multiple times with Bridgeless/ReactHost stack.
- Mapped bundle stack offsets (`anonymous@7658`, `@7615`, `global@397683`) to RN core init path (`NativePlatformConstantsAndroid` during `InitializeCore`), i.e. crash occurs before app JS handlers run.

Commit-focused investigation (per request):
- Reviewed last 3 commits:
  - `df44ee0` (alarm queue / timeout)
  - `7a1b803` (missed-state + schedule fixes)
  - `112f45f` (usage gate + hydration)
- These touched app/JS logic and `app.json` version code, but not Android host/bootstrap wiring.

Isolation tests performed:
- Created temp worktree and reverted plugin commit `bf7bdeb` only.
- Result: build **fails** on current stack (Kotlin/API mismatches in generated native files), so `bf7bdeb` is currently required for compilation.
- Created second temp worktree at `4754563` (before last 3 commits), installed, then cold-start tested: **5/5 OK**.
- Reinstalled current `HEAD` (`versionCode=9`), cold-start tested: **5/5 OK**, then crash still reappeared later (16:18:42), confirming intermittency.

Current understanding:
- Failure appears to be an intermittent RN bridgeless/native startup race/state issue, not a deterministic regression from a single one of the last 3 commits.
- Need tighter trigger conditions (exact launch path/device state/timing) to isolate further.

Files touched / investigated:
- `updates/2026-02-14_devlog.md` - this entry
- `app.json`
- `index.ts`
- `alarm/AlarmRoot.tsx`
- `lib/notifications.ts`
- `plugins/withFullScreenAlarm.js`
- `plugins/withAlarmAudioModule.js`
- `android/app/src/main/java/com/wahabbasa/VoiceReminder/MainActivity.kt`
- `android/app/src/main/java/com/wahabbasa/VoiceReminder/AlarmActivity.kt`
- `android/app/src/main/java/com/wahabbasa/VoiceReminder/MainApplication.kt`
