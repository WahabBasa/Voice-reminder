# Devlog 2025-12-15

## Startup routine - (02:35)

**Status**: OK

- Checked date/time: 2025-12-15 02:35
- Read plan.md (Phase 1-2 done; Phase 3+ pending)

## Changes in this session

### Reminder data + Convex sync
- Normalized reminder frequency to `custom` (server previously returned `weekly`).
- Added `convexId` on locally-stored reminders so edits/deletes can sync back to Convex.
- Fixed calendar day-key mismatch by using lowercase keys (`sun`..`sat`) when filtering reminders.

### Notifications + audio playback
- Added `soundRepeatMode` / `soundRepeatCount` (stored locally and passed through Notifee data payload).
- Updated notification event handler to replay reminder audio N times (with a small gap) and wait for playback to finish.

### Reminder UI (new/edit/detail)
- Added "Sound repeats" picker (1x/2x/3x/Until stopped) to reminder detail + edit + new flows.
- New/Edit now persist repeat settings + reschedule notifications with the updated repeat settings.
- Home header bell now opens a modal listing scheduled reminders (time/frequency + description).

### TTS backend
- Switched TTS generation in `convex/actions.ts` from OpenAI to ResembleAI `POST https://f.cluster.resemble.ai/synthesize`.
- Uses `RESEMBLE_API_KEY` and `RESEMBLE_VOICE_UUID`; `RESEMBLE_PROJECT_UUID` is optional (auto-selects the first project if omitted).
- Added `processTextReminder` so the "New reminder" screen can create a reminder without voice (still generates TTS server-side).

### Time picker
- Improved `components/ScrollSelector/index.tsx` to better support looping + snapping/settling for hours/minutes while keeping AM/PM non-looping.

### Housekeeping
- Added `tmp/` + a couple local scratch patterns to `.gitignore`, and excluded `tmp/` from `tsconfig.json`.

## Next up
- Adopt the wheel behavior from `troberts-28/react-native-timer-picker` (FlatList + `snapToOffsets`) for the reminder time sheet.
