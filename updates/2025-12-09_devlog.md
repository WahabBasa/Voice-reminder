# 2025-12-09 Devlog

## Session Start - (16:08)

**Status**: Starting session

Ran startup routine. Last devlog was 2025-12-04 (5 days ago).

---

## Major UI Redesign + Offline-First Architecture - (16:30-20:00)

**Status**: ✅ Complete

Big session - redesigned UI to match medicine-reminder-app reference and converted to offline-first.

### Changes Made:

**1. Offline-First Storage**
- Created `lib/storage.ts` with AsyncStorage CRUD for reminders and history
- `getReminders()`, `addReminder()`, `updateReminder()`, `deleteReminder()`
- `recordCompletion()`, `getTodaysHistory()`, `getHistory()`, `clearHistory()`
- App now works without internet (only voice processing needs connection)

**2. New Pages**
- `app/calendar.tsx` - Month grid view, day selection, activity dots
- `app/history.tsx` - History log with filter pills (All/Completed/Missed)
- `app/reminder/edit.tsx` - Full-screen edit with slide-from-bottom animation
- `app/reminder/new.tsx` - Manual text entry for reminders

**3. Navigation Overhaul**
- Removed BottomSheet for edit/new - now full-screen modals
- Added `slide_from_bottom` animation for reminder screens (looks like sheet)
- Kept `slide_from_right` for calendar/history pages
- Voice recording stays as modal overlay

**4. Calendar Bug Fix**
- Reminders now filter by day based on frequency:
  - `daily` = shows every day
  - `custom` = shows only on selected days (Mon, Tue, etc.)
  - `once` = shows only on creation date

**5. Completed Reminders**
- Mark as done → reminder disappears from Today's Schedule
- Shows "All done for today!" when everything completed
- Reminders reappear next day for daily/custom

**6. Custom Scroll Wheel**
- Created `components/ScrollSelector/` (index, HighlightView, Placeholder, SelectedItem)
- Updated `TimePicker.tsx` to use custom scroll wheel
- Note: Scroll wheel inactive inside some contexts - left as-is for now

### Files Created:
- `lib/storage.ts`
- `app/calendar.tsx`
- `app/history.tsx`
- `app/index.tsx` (moved from tabs)
- `app/reminder/edit.tsx`
- `app/reminder/new.tsx`
- `components/ScrollSelector/*`

### Files Modified:
- `app/_layout.tsx` - New navigation structure
- `components/TimePicker.tsx` - Custom scroll wheel
- `components/ReminderCard.tsx` - Done button styling
- `components/DetailSheet.tsx` - Added onDelete prop
- `lib/theme.ts` - Blue gradient colors
- `package.json` - Added @react-native-community/datetimepicker

### Files Deleted:
- `app/(tabs)/` folder - Flattened structure

---
