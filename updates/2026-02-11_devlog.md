# Devlog - 2026-02-11

<!-- Created by startup routine. Add entries below when needed. -->

## Fixed reminder lifecycle edge cases + scheduling debug - (12:04)

**Status**: ✅ Behavior corrected, builds passing

Tracked down two reminder regressions and patched both in app logic:

- One-time reminders were disappearing from both **All** and **Completed** if they fired and were ignored.  
  Now they stay visible in **All** until explicitly completed, and overdue one-time reminders are auto-recorded as `missed` in history during startup sync.
- The home Completed feed was filtering only `completed`; it now includes both `completed` and `missed`.
- Repeating reminders created on the same day after their configured time (example: Sunday 7:00 AM created Sunday 9:00 AM) were showing as immediately delayed.  
  `getDueTimestamp()` for daily/weekly/custom now always resolves to the next future occurrence.
- Added a scheduling debug log in `scheduleReminder()` to print source, schedule type, now/trigger timestamps, and delta minutes.

Validation:
- Ran `npx.cmd tsc --noEmit` after each patch set (pass).

Files touched:
- `lib/reminderActive.ts` - active/cleanup behavior for one-time reminders
- `app/index.tsx` - include `missed` entries in completed feed
- `lib/notifications.ts` - auto-mark missed one-time reminders on startup + schedule debug log
- `lib/store.ts` - added `auto_missed` history action type
- `lib/time.ts` - future-facing due calculation for daily/custom/weekly

## Implemented single-active alarm queue + 3-minute timeout policy - (15:22)

**Status**: ✅ Implemented and typecheck passing

Added the “good enough” alarm policy for production behavior:

- Only one alarm is active at a time.
- New due alarms while one is active are queued FIFO.
- Ring timeout is now 3 minutes.
- Ignore timeout auto-snoozes once (+5 min), then marks missed on next timeout.
- Added stale/internal notification-event guards so old dismiss/action events can’t clear active alarms.

Also fixed activity handoff so when one alarm resolves and another is queued, the alarm task stays alive and immediately promotes the next alarm instead of closing the activity.

Validation:
- Ran `npx.cmd tsc --noEmit` after integration (pass).

Files touched:
- `lib/notifications.ts:56` - alarm queue key + timeout constants
- `lib/notifications.ts:627` - `handlePendingAlarmTimeout()` (3m timeout, one auto-snooze, then missed)
- `lib/notifications.ts:701` - `enforcePendingAlarmTimeout()` polling/timer sync
- `lib/notifications.ts:1232` - DELIVERED flow queues overlapping alarms
- `lib/notifications.ts:1360` - stale ACTION_PRESS guard
- `alarm/AlarmRoot.tsx:60` - keep AlarmActivity open when queued alarm is promoted
- `alarm/AlarmRoot.tsx:140` - enforce timeout in alarm poll loop
- `app/_layout.tsx:139` - enforce timeout in foreground fallback poll loop
- `components/AlarmOverlay.tsx:60` - added `autoSnoozeCount` to overlay props
- `app/alarm.tsx:315` - preserve `autoSnoozeCount` through snooze reschedule
- `app.json:20` - Android `versionCode` set to 9
