# Devlog 2026-01-04

## Session start - (08:00)

**Status**: 🟡 Performance investigation ongoing

**Known issue**: Bottom sheet is still slow and requires double taps to open. All optimizations below haven't fully resolved it.

---

## Performance optimizations applied - (20:25 on Jan 3 → 08:04)

Received detailed performance reports identifying JS thread blocking as the root cause of tap lag. Applied several optimizations:

### A. Sort optimization in `index.tsx`

Pre-compute timestamps before sorting to avoid O(n log n) Date constructions:

- `remindersBySection` (line 464): now uses `filteredReminders.map(r => ({ reminder: r, ts: new Date(r.createdAt).getTime() }))` then sorts by `ts`
- `completedBySection` (line 485): same pattern for history entries
- `completedTodayReminderIds`: uses numeric timestamp bounds instead of `toDateString()` string comparison

### B. Locale formatting cache in `time.ts`

Added LRU caches (100 entries each) for expensive `toLocale*` calls:
- `cachedTimeString()` - caches `toLocaleTimeString`
- `cachedWeekdayString()` - caches weekday formatting
- `cachedShortDateString()` - caches short date formatting

### C. Load yielding in `index.tsx`

Changed `loadData()` to load reminders first, then defer history load via `InteractionManager.runAfterInteractions()` so taps can interleave.

### D. Slider gesture fix attempt

Added responder capture to slider containers in `edit.tsx` and `DetailSheet.tsx`:
```tsx
onStartShouldSetResponder={() => true}
onMoveShouldSetResponder={() => true}
onStartShouldSetResponderCapture={() => true}
onMoveShouldSetResponderCapture={() => true}
```

Removed the `NativeViewGestureHandler` wrapper that wasn't working.

**Result**: These optimizations reduced some JS work but **did not fix the double-tap issue**.

---

## Performance instrumentation for tap → edit sheet delay - (08:04)

**Status**: 🟡 Instrumented + ready to repro

Since optimizations alone didn't fix the issue, added comprehensive perf tracing to diagnose exactly where the delay occurs.

### What's instrumented:

**A. Home screen tap tracing (`index.tsx`)**
- `tapDebugSnapshotRef` captures state at tap time (reminders count, history count, isLoading, etc.)
- `recordReminderPressIn()` logs `reminder_press_in` with traceId
- `handleReminderPress()` logs `reminder_press` and `router_push_edit`, passes traceId to edit screen

**B. JS stall monitor (`lib/perf.ts`)**
- `requestAnimationFrame` loop detects when event loop stalls > 100ms
- Correlates stalls with last tap traceId
- Enable with `EXPO_PUBLIC_VR_STALL_MONITOR=1`

**C. Storage timing (`lib/storage.ts`)**
- `getReminders()` and `getHistory()` log:
  - `getItem_start`, `getItem_done` (with ms duration)
  - `parse_start`, `parse_done` (with ms duration, byte count, item count)

**D. Edit screen lifecycle (`app/reminder/edit.tsx`)**
- `screen_mount` - when component mounts
- `screen_first_frame` - first requestAnimationFrame after mount
- `runAfterInteractions_scheduled` / `_start`
- `loadReminder_start` / `_reminders_loaded` / `_done`
- `audio_preload_scheduled` / `_start` / `_done`
- `bottomSheet_onAnimate` / `_onChange`

### How to run:

```powershell
# Enable perf logs
$env:EXPO_PUBLIC_VR_PERF_LOGS="1"

# Enable stall monitor (optional)
$env:EXPO_PUBLIC_VR_STALL_MONITOR="1"

# Start Metro (port 8082 if 8081 is occupied)
npx.cmd expo start --clear --port 8082
```

Files changed:
- `app/index.tsx` - tap tracing + history loading scheduling
- `app/reminder/edit.tsx` - edit screen + bottom sheet trace logging
- `lib/storage.ts` - AsyncStorage + parse timing logs
- `lib/perf.ts` - stall monitor + tap attribution helpers
- `lib/time.ts` - cached locale formatting helpers
- `components/DetailSheet.tsx` - slider responder capture

---

## Known issues still outstanding

1. **Double-tap required to open bottom sheet** - First tap sometimes doesn't trigger navigation, second tap works. Cause unknown, needs trace analysis.
2. **Bottom sheet animation feels sluggish** - Even when it opens, the animation feels slow (though timing config is 280ms).
3. **Slider may still have gesture conflicts** - Responder capture attempted but not verified on device.

## Next steps

1. Run app with perf logs enabled
2. Reproduce the double-tap issue
3. Analyze logs to determine if delay is:
   - Before `onPressIn` (JS thread blocked)
   - Between `onPressIn` and `onPress` (gesture/scroll interference)
   - After `router.push` (navigation/mount blocked)
4. Apply targeted fix based on findings

---

## Session 2 - Performance Profiling & Overlay Implementation (10:00 → 16:50)

### React DevTools Profiler Analysis

Used React DevTools Profiler to capture render times when opening the edit sheet:

| Component | Render Time |
|-----------|-------------|
| BottomSheet (ForwardRef) | 23-33ms |
| edit.tsx | 10.5ms |
| BottomSheetContent (Memo) | 6.3ms |
| BottomSheetScrollView | 5.9ms |
| SVG icons (multiple) | ~0.4-0.7ms each |

**Key finding**: The ~500ms delay was NOT React rendering (which is fast at ~23ms). It was **Android native modal creation** from the `transparentModal` presentation.

### Fixes Applied

1. **Removed InteractionManager delay** in `edit.tsx` - data loads immediately on mount
2. **Removed custom spring animation config** - reverted to snappier defaults
3. **Added missing import** `DEFAULT_ALARM_SETTINGS` to `index.tsx` (was causing crash)
4. **Fixed duplicate code** in `loadReminder` function
5. **Passed reminder data via URL params** to avoid "Loading..." flash

**Result**: Double-tap issue fixed, but ~500ms modal delay remained.

### Overlay Implementation

Created new overlay approach to bypass Android modal creation delay:

**New files:**
- `components/EditReminderSheet.tsx` - standalone BottomSheet component

**Modified files:**
- `app/index.tsx` - renders EditReminderSheet as overlay instead of navigating

**How it works:**
```
Before: Tap → router.push() → Android modal (500ms) → BottomSheet
After:  Tap → setEditingReminder() → BottomSheet (instant)
```

### Known Issues (Outstanding)

1. **Still some lag** - Even with overlay approach, sheet opening isn't instant. May need further profiling.
2. **Volume slider not working** - Gesture conflict with BottomSheet scroll.
3. **FAB stays visible** - Record button should hide when edit overlay opens.
4. **Original edit.tsx route** - Still exists but no longer used for tap navigation (kept for deep linking).

### Files Changed This Session

- `app/index.tsx` - overlay state + EditReminderSheet rendering
- `app/reminder/edit.tsx` - removed InteractionManager, fixed duplicates
- `app/_layout.tsx` - adjusted animation duration
- `components/EditReminderSheet.tsx` - NEW overlay component

---

## Edit Sheet UI Polish - (18:23)

**Status**: ✅ Done

Quick polish pass on the edit bottom sheet:

- FAB now hides when edit sheet opens, reappears with smooth fade animation
- Fixed volume slider - works smoothly now (local state for dragging, debounced saves)
- Cleaned up layout: volume section with label on top, slider below
- Renamed "Voice Sound" → "Play reminder", "Regenerate Sound" → "Regenerate Reminder"
- Removed redundant elements: frequency chip + three-dots menu from sheet header
- Added quick reconnect instructions to `CLAUDE.md`

Files: `components/EditReminderSheet.tsx`, `app/index.tsx`, `CLAUDE.md`
