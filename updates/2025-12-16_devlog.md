# Devlog 2025-12-16

## Startup routine - (01:04)

**Status**: OK

- Checked date/time: 2025-12-16 01:04
- Read plan.md (Phase 1-2 done; Phase 3-6 pending)
- `cloud.md` not found in repo; used `CLAUDE.md` for startup steps

## AM/PM wheel time picker - (03:22)

**Status**: Working

Patched `react-native-timer-picker` so when `use12HourPicker` + `useAmPmWheel` are enabled it shows 3 wheels (Hour / Minute / AM-PM) by repurposing the seconds wheel.

- Added `patch-package` + `postinstall` so the library patch persists across installs
- Updated reminder time pickers to convert `{ hours (0-11), minutes, seconds (0|12) }` back into a 24h hour before setting the `Date`
- README note: to enable the picker fade-out in Expo, install `expo-linear-gradient` and pass `LinearGradient={LinearGradient}` into `TimerPickerModal` / `TimerPicker` (we’re doing this in the reminder pickers)

Files:
- `app/reminder/new.tsx:214` - TimerPickerModal now uses `useAmPmWheel` + AM/PM conversion
- `app/reminder/edit.tsx:348` - TimerPickerModal now uses `useAmPmWheel` + AM/PM conversion
- `components/DetailSheet.tsx:298` - updated to match the new AM/PM wheel behavior
- `patches/react-native-timer-picker+2.4.0.patch` - library patch (AM/PM wheel + display tweaks)
- `package.json` - `patch-package` + `postinstall`

## Switch reminder TTS to ElevenLabs - (11:02)

**Status**: Working (after upgrading ElevenLabs plan)

Swapped the Convex TTS generation from Resemble to ElevenLabs for reminder audio, but kept a fallback path so we can still generate audio if ElevenLabs isn’t configured.

Notes:
- Initially hit `ElevenLabs TTS failed (401) ... detected_unusual_activity / Free Tier usage disabled` when creating a voice reminder; resolved after subscribing (paid plan) and retrying.
- Added `.env.example` and updated `plan.md` to reflect ElevenLabs as the TTS provider + document required env vars.

Files:
- `convex/actions.ts` - added ElevenLabs TTS + provider selection (`TTS_PROVIDER`, `ELEVENLABS_*`)
- `.env.example` - env template (client vs Convex secrets)
- `.env.local` - placeholder for `EXPO_PUBLIC_ELEVENLABS_AGENT_ID` (no secrets)
- `plan.md` - marked Phase 3-5 completed and updated TTS docs

## AM/PM wheel: disable infinite scroll - (18:03)

**Status**: Working

The AM/PM wheel was still behaving like an infinite/circular picker. Updated our `patch-package` patch to force the meridian wheel to be non-looping (just scroll up/down between AM and PM).

- `patches/react-native-timer-picker+2.4.0.patch` - for the AM/PM wheel path, force `disableInfiniteScroll: true` and `repeatNumbersNTimes: 1`
