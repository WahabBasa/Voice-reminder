# Devlog - 2026-08-21

## video-toolkit: four tools built in parallel Opus lanes - (day)

**Status**: ✅ all validated vs ground truth (OLD-112..115 In Review)

New repo `C:\Dev\video-toolkit` — deterministic tools so agents judge instead of
grind. One workflow, 4 lanes, ~630k tokens, ~3.4h under CPU contention:

- `map.py` (31 tests): scenes/audio/static spans → map.json + stamped contact
  sheets, 129s for a 5.5-min video. ffmpeg scdet score track beat BOTH
  PySceneDetect (missed the 298.4s banner) and auto-editor; hwaccel decode was 3x
  slower (readback), skipped.
- `pii.py` (38 tests): RapidOCR pipeline (2.2 s/frame; EasyOCR aborted at 900s;
  no Tesseract binary on host) finds a target string → time-windowed blur rects.
  All 8 email sightings found incl. one living between 1-fps samples; corrected
  two ground-truth windows with dense-scan evidence. New dep: rapidocr-onnxruntime.
- `render.py` (36 tests): edit-spec JSON → verified mp4. Reproduced the 08-20 edit:
  master byte-identical, final within one frame. AMF (Vega 8) benched and
  rejected — cqp only 1.29x faster at +25% size, qvbr VMAF 84 → libx264 stays.
- `point.py` (55 tests): Clicky-pattern coordinates ([RECT:x,y,w,h:label]).
  Live-validated via OpenRouter (IoU 0.78-0.80, correct refusal on absent target).
  Unresolved: no Anthropic key on this machine; anthropic transport written,
  never executed. Upstream finding: Clicky's Worker holds no prompt — protocol
  lives client-side; RECT is our extension.

Economics: hand-run pipeline was ~90 min / ~400k tokens; projected with tools
~20 min / 30-50k. Acceptance run = OLD-116, pending.

## Script project: loom→ENIAC documentary started - (evening)

Fern script + P2W-essayist script dissected → `docs/content/fern-script-playbook.md`.
Fact base verified (loom lineage, Lovelace verbatim, ENIAC specs, first-computer
caveats) → description-notes block in the script file. Drafts v1→v7 iterated in
`docs/content/scripts/the-pattern-and-the-machine.md`. Narration pipeline probed:
`docs/speechify-ssml-dialect.md` — Accept: audio/pcm is mandatory (else silent MP3
disguised as PCM = "dead radio"), breaks land at half value, bare rate % speeds UP
(negatives slow down), no pause engineering — punctuation paces best.
