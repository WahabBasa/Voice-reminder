# Devlog - 2025-12-03

## Got Phase 1 working - (19:30)

**Status**: ✅ App running on phone

Started the Expo project with Convex backend. Ran into a few things:

- Windows path was too long so the build kept failing. Moved everything to `C:\Dev\VR` and it worked.
- React 19 has peer dep issues with some packages. `--legacy-peer-deps` on every install.
- Notifee needs its own maven repo. Added it to `android/build.gradle:20`.
- Phone couldn't hit localhost for Metro. Used `adb reverse tcp:8081 tcp:8081` since it's USB anyway.

**Files created:**
- `app/_layout.tsx` - wraps everything in ConvexProvider
- `app/(tabs)/_layout.tsx` - two tabs: Reminders and New Reminder
- `app/(tabs)/index.tsx` - home screen with empty state
- `app/(tabs)/record.tsx` - mic button, toggles between record/stop

**Deps:** convex, @notifee/react-native, expo-av, expo-file-system, openai, @expo/vector-icons

Running on Samsung SM_G955F. Both tabs work. Convex dashboard: https://dashboard.convex.dev/d/proper-stoat-767

---

## Set up project docs - (20:00)

**Status**: ✅ Done

- Created `CLAUDE.md` with startup routine and how to write these devlogs
- Made `updates/` folder for devlog files  
- Updated `plan.md` to show Phase 1 is done

Git repo moved to `C:\Dev\VR`, pushed to GitHub. `.env.local` is gitignored.
