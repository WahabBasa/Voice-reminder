# Devlog - 2026-08-25

## Lock-screen Done/Later verified on device; "bug" was dev-tooling ghosts - (16:20)

**Status**: ✅ OLD-110 + OLD-111 verified end-to-end on iPhone 12 (iOS 26.6, 23G71)

Ran the instrumented dev build (d469c54d) live: Metro over the Galaxy hotspot for JS
logs + `pymobiledevice3 syslog live` over USB for the native tape (works on Windows;
needs `PYTHONIOENCODING=utf-8` or it dies on U+202F in Apple timestamps). The 24th's
attempt stalled on hotel Wi-Fi client isolation — hotspot fixed it.

What the tape proved, locked phone, no unlock at any point:

- **Later**: `mobiletimerd` → `VRSnoozeIntent.perform()` **in the Remi app process**,
  ring silenced in 60ms (`stopRinging source=param`), snooze guard written, 1.1s total.
- **Done** (slide-to-stop on the +5 comeback): `VRStopIntent perform start` with
  `snoozeGuardMillis=-1` (guards are per-appKey — comeback's Done can never be
  swallowed by the ring's Later guard) → `recorded=stopped`, 120ms.
- **No Face ID, no app open.** Control test with Settings frontmost before locking:
  Settings still frontmost after answering. "Remi was open" earlier = last-used app.
- History shows the completions → reconciliation works too.

The "events vanish" scare mid-session was the dev client itself: on foreground the OLD
JS context drains the native event log and processes it (its logs die with its dead
Metro socket — only the `reminderRemoval.ts` lazy-bundle HTTP request stays visible),
then the client reloads and the NEW context drains 0. Production has no reload; not a
product bug. Kept the `drained_events` telemetry line — it's what cracked it.

- `plugins/ios-src/VRAlarmIntents.swift` — os_log instrumentation + explicit
  `supportedModes = [.background]` (metadata-identical to build 2, see 08-23)
- `lib/notifications.ts:1923` — unconditional `drained_events` log on every drain

Perf number captured on the way: micStop→card **7.3s** = upload 1.5s +
convexAction 5.6s (STT + GPT parse dominate; TTS already off the critical path).

### Next Session

1. Voice-processing latency (OLD-82/OLD-106): attack the 5.6s convexAction. Sort
   real vs perceived — optimistic card from transcript, streaming STT; user open to
   swapping the STT model, NOT the parse model. (Speechify is TTS — already ours,
   not part of this path.)
2. 2.1 resubmit: re-record demo WITH mic on the TestFlight build, reply in Resolution
   Center AND resubmit (see 08-22 devlog for the full flow).
3. Close OLD-110/OLD-111 in Linear (done this session), sweep the ~34 In Review.
4. Carry: OLD-116 video-toolkit skill, shot-1 reshoot, Arabic (Saudi) listing,
   RevenueCat "no packages for offering default" warning while subs sit frozen with
   the rejected version — recheck after approval.
