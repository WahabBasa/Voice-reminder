# Devlog - 2026-08-19

## Lock-screen alarm answers fixed: Later silences, Done stays locked - (07:30)

**Status**: ✅ Code + tests green · ⏳ device verification rides the production build

User repro'd two bugs on the same surface. (1) Tapping Later kept the alarm ringing
(the comeback chain itself worked). Cause: the ring's audio only ever died as a side
effect — pre-OLD-96, Later always rescheduled its own app key and guard-5 rotation
cancelled the alerting UUID on the way. The pre-scheduled chain skips that reschedule
whenever comebacks are owed (the normal case now), and nothing in the native layer ever
called `AlarmManager.stop`. (2) Slide-to-stop demanded Face ID and shoved the user into
the app: `openAppWhenRun=true` on VRStopIntent. Foregrounding only bought immediate
event-log drain — reconciliation runs on every foreground anyway, grid reminders
pre-register 4 occurrences, and the JS completed-branch is documented as the backstop
for intents that never ran. Not load-bearing, so it's gone.

- `plugins/ios-src/VRAlarmIntents.swift` — snooze perform() stops the alerting UUID
  right after the guard-1 write (stop, never cancel — registry survives for Done's
  sibling bookkeeping), with a registry fallback when the UUID param is lost;
  `VRStopIntent.openAppWhenRun` → false
- `plugins/withAlarmKit.js` — `VRAlarmScheduler.stopRinging(uuid:)` on the AK-1 seam;
  placeholder intent flipped to match
- `__tests__/plugins/alarmIntentsSource.test.ts` — 6 new source-contract tests, red first
- `docs/alarmkit-test-plan.md` step 4 — Done must now keep the phone locked

163 tests green across the five alarm suites, tsc clean. Both fixes are native — they
land with the production build. Device check: Later cuts the audio instantly; Done never
prompts for unlock.

## ASO library + full ASC screenshot set generated - (10:30)

**Status**: ✅ 5 shots ASC-ready in `marketing/out/asc/` (6.7" + 6.5")

- `docs/aso/transcripts/` — two user-shared transcripts distilled + cleaned (ASA
  tier-2/3 playbook; organic ASO keywords/locales), README index. Competitor review
  corpus themed at `docs/aso/competitor-reviews-talking-alarm-clock.md`. CLAUDE.md
  points at all of it.
- Screenshot set generated GPT-side from structured prompts, style locked on shot 1:
  ice-blue gradient, editorial serif with one italic word, frontal flat device, pop-out
  speech bubbles, emoji chips. Captions rebuilt around spoken-vs-silent-notification
  after the user killed the productivity-copy direction. Flow: GPT renders land in
  `Downloads\LANDrop` → approved masters to `marketing/renders/01..05` → new
  `marketing/upscale_to_asc.py` emits exact 1290x2796 + 1242x2688.
- Gotchas: GPT caps output at 852x1846 regardless of asked size — Lanczos 1.51x is
  clean on this flat style. Slot-1 mis-file caught (grabbed tilted v1 from Downloads;
  approved final was in LANDrop) — refiled, re-upscaled.
- Upload: the 6.7" five into ASC's 6.9-inch slot (1290x2796 is accepted there), order
  01→05; 6.5" set only if ASC offers the extra slot.

## 1.0 submitted for App Store review - (13:20)

**Status**: ✅ Waiting for Review · build 2 · manual release

Build → submit → ASC gauntlet in one sitting:

- EAS production build first died headless: "Distribution Certificate is not validated
  for non-interactive builds". One interactive `npx.cmd eas-cli build --platform ios
  --profile production` run (Apple ID + 2FA) validated it permanently; `eas submit`
  then generated + stored an ASC API key. Both commands run non-interactive from now
  on. Cert + provisioning profile expire 2027-08-07.
- autoIncrement bumped `expo.ios.buildNumber` 1→2 (`app.json`, this commit — eas-cli
  also re-indented the privacy-manifest arrays, no content change).
- `eas submit --latest` auto-created the TestFlight group and enabled
  wahabbasa@gmail.com — zero manual tester setup.
- ASC blockers surfaced one at a time on Add for Review: Privacy Policy URL (the live
  GitHub Pages one from `lib/legalLinks.ts:19`, verified 200) and Content Rights (no
  third-party content). Screenshots: the 6.9" slot took the 1290x2796 set and the 6.5"
  slot auto-falls back to it — the 6.5 upscales never needed uploading.
- Manual release is set, so approval ships nothing by itself — release is a deliberate
  button press after the TestFlight pass.

### Next Session
1. TestFlight device pass on build 2 while review runs: Later cuts audio instantly,
   Done never prompts unlock, paywall shows "Remi Pro" with both plans at real prices.
   Red = cancel the submission before release.
2. On approval, before pressing Release: confirm both subs (and the 7-day intro on
   vr_pro_annual) actually rode along and got approved with the version — ASC lets you
   submit without them, and the attach step was never explicitly confirmed.
3. Reshoot the lock-screen capture on the Remi build → rerun shot-1 prompt → refile →
   swap in ASC (kills the "VoiceReminder" label in shot 1). Decide paywall shot 6.
4. Arabic (Saudi) locale listing per the playbook.
5. RC cleanup: archive stale `default` offering (pending approval); runbook Stage 4
   rewrite; verification sweep + In Review closeout (~30 issues).
6. Carry: stop-button semantics, live evals, alarm.tsx debug route, DatePickerModal dead
   half, typedTake coverage, unused `persistent` validator arg, TTS key rotation, OLD-109.
