# Devlog - 2026-08-13

## Provider scrub, RevenueCat truth, Sentry latency root cause - (08:01)

**Status**: ✅ all fixed, 700/700 tests · 🔒 publish still held (coupled convex+OTA batch ready)

Three threads today, all Opus-agent work:

- **Provider names scrubbed** (user directive — stack stays under wraps): AiConsentSheet and the
  mic permission string now say "secure AI services", zero company names anywhere user-facing.
  One string finished by hand after the workflow stalled (see below).
- **RevenueCat mystery solved twice.** The recompile errors: `PLACEHOLDER_IOS_KEY` made the SDK
  throw on every call. But the deeper truth via the new RevenueCat MCP (registered user-scope,
  tools live next session): the RC project is literally named **NoteToSelf** and has **no App
  Store app at all** — no `appl_` key exists to paste. `lib/purchases.ts` now gates configure on
  `isUsableKey()` (iOS skips entirely, no more error spam), and account cleanup is queued for
  next session: create the App Store app, VoiceReminder-named products, dedupe the
  `default`/`default1` offerings. Secret key rotated twice after transcript exposures.
- **Sentry latency (OLD-82) — user called it, mechanism was a trap**: `tracesSampleRate: 0`
  ENABLES tracing in @sentry/react-native (the gate is `typeof === 'number'`, and 0 is a number).
  So "no performance tracing noise" actually meant: 50ms stall-poll loop, XHR patching around the
  audio upload and every Convex call, native frame tracking — all discarding their data. Plus
  console breadcrumbs = one sync native hop per log line on the app's loudest path. Both killed;
  RC log spam filtered via beforeSendLog; kept Sentry Logs streaming in dev (Metro can't reach
  the phone — it's the only window). New `[VR PERF SUMMARY]` one-liner per run + server-side
  perf split from processVoiceReminderFast. Co-suspect instrumented, not changed: the
  `playsInSilentModeIOS` session switch on mic-stop landed in the same commit — `stopRecording`
  now splits unloadMs vs audioModeMs so the next device run settles it.

Workflow gotcha: the debrand+RC workflow's agents finished their edits but the run hung ~80min
and the task died without a completion notification. Edits verified clean (tsc + 700/700).

### Next Session

1. **PUBLISH (coupled)**: `npx convex deploy` + OTA together — the deviceId change breaks old
   clients against the new backend and vice versa. Then force-quit app twice to apply.
2. Device run: record 3 reminders → read `[VR PERF SUMMARY]` / `stopRecording_split` /
   `convex_perf` in Sentry → Explore → Logs (full procedure on OLD-82). audioModeMs triple-digit
   → new ticket for the audio-session switch.
3. RevenueCat via MCP (tools live in fresh session): App Store app + real `appl_` key into
   lib/purchases.ts, offering dedupe, VoiceReminder product naming. ASC side stays manual
   (IAP products, Paid Applications Agreement, banking/tax).
4. Go over remaining pre-publish map tickets with user: OLD-64 device verification (ship gate),
   OLD-66/67 Speechify decisions, OLD-73 privacy policy (rewrite + host), OLD-79 labels/manifests,
   OLD-80 Live Activity decision, OLD-81 processing animation.
5. Pending user yes: typed-reminder consent-gap ticket + storage-upload abuse-endpoint ticket.
6. Carry-overs: OLD-58/59 emoji verification on device, Apple Feedback on attended presentation,
   drawer card shadow, dark status-bar icons.

## Continued — publish out, prompt round 2, live evals, two-beat catches - (09:11-late)

**Status**: ✅ coupled publish live · ✅ eval harness built · ✅ catch feature 829/829 (not deployed tonight)

- **Publish shipped.** `convex deploy` (prod) fine from agent shell; `eas update` died with the Metro
  SHA-1 error even WITH `--clear-cache` when launched from a Git Bash-parented cmd (error path shows
  lowercase `c:\`). GPT ran it from a real terminal: update group 2b6460fc. Memory updated: publish
  needs a directly-opened terminal, agent shells can't do it. **Key wiring fact:** the phone talks to
  DEV (`proper-stoat-767`, via .env.local) — prod deploys never reach it. Dev push + OTA is the real
  couple; both went out.
- **OLD-61 round 2.** Dev-table dig showed the full history on one input: "Time to drink water" →
  "Quick reminder — it is time..." → byte-identical "It is time to drink some water." twice. Post-fix
  line dodged the ban by padding ("Hydrate yourself with a fresh glass of water now"). Ban now covers
  label openers too; budgets 4-9 desc / 4-10 variants; no wellness lectures. Titles must be opener-free
  (both inline prompts).
- **Live eval harness**: `__evals__/reminder-phrasing.eval.ts`, `npm run eval` — 5 transcripts × 3
  samples through the exact shipped prompt (`buildSystemPrompt` exported), lints openers/budgets/
  greetings/schema echoes/ladder variety, prints every generated line. Across 4 runs: English 100%
  clean; Arabic leaks `حان وقت` in ~1/6-1/3 of samples — systematic, prompt ban not landing.
- **Two-beat catches** (design grilled with user: address-the-person catch, then payload — "Heads up,
  Wahab — your meeting..."). Catch is 100% code-assembled at TTS time from approved pools (EN 10 +
  AR 7 + name combos; persona rule: the assistant never forgets), rotation never repeats
  (`speechCatchState` table, atomic claim in reminders.ts), first firing + heads-up only, ladder rungs
  stay bare, stored description untouched. `convex/speechCatch.ts` + 57 tests.
- **Opener guard** shares `BANNED_OPENERS` with the eval. Fresh-eyes review caught a nasty floor bug:
  ordinary titles ("Time to Sleep", "تذكير بالدواء") are themselves banned → guard returned "" → blank
  TTS on fast path, reminder never created on slow path. Fixed: guard only trades up, never empties;
  heads-up fallback checks the title; catch language = majority script with the address term excluded
  (Arabic name no longer flips an English line to an Arabic catch). tsc clean, 829/829.
