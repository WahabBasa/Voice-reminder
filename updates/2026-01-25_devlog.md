# Devlog - 2026-01-25

## Session Start - (10:13)

**Status**: バ. Ready to develop

Ran startup routine. Repo up to date. Phases 1-6 complete, 7-9 partial, 10-11 pending.

---

## Standardized in-app sheets + interval cadence wiring - (14:21)

**Status**: OK. Typecheck clean, Metro boots

Main user-facing issue was "Repeat"/"Custom repetition" showing native system dialogs instead of in-app UI. I standardized choice-based "action sheets" into consistent in-app bottom sheets, and fixed the layering problem where sheets can appear behind a React Native `<Modal>`.

What changed (high level):
- Added a portal layer so sheets always render above the app (and above RN modals).
- Replaced choice-based `Alert.alert(...)` pickers/menus with `ActionSheet` + `PickerSheet` (kept single-button error/info alerts as-is).
- Interval reminders are now first-class in scheduling + parsing: `intervalMs`, `anchorAt`, `scheduledFor`, stable cadence based on scheduled occurrence time (not `Date.now()` at delivery).

Notes / gotchas:
- For sheets opened from inside RN `<Modal>`, I mount a `PortalHost` inside that modal and pass its `hostName` into the sheet so it always overlays correctly.
- Expo `--non-interactive` isn't supported; using `CI=1` is the supported way. Metro started fine on an alternate port (`8082`) to avoid conflicts.

Checks run:
- `npx.cmd tsc --noEmit`
- `npx.cmd expo start --port 8082` (started Metro; local run only)

Files touched (key spots):
- `app/_layout.tsx:8` - added `PortalProvider` at the root so portals work app-wide.
- `components/ActionSheet.tsx:92` - `BottomSheet` rendered inside `<Portal hostName=...>` (new file).
- `components/PickerSheet.tsx:186` - picker bottom sheet rendered inside `<Portal hostName=...>` (new file).
- `components/RepeatTaskModal.tsx:243` - added `<PortalHost name={portalHostName} />` inside RN `<Modal>`; interval selection now uses `PickerSheet`.
- `components/DatePickerModal.tsx:319` - added `<PortalHost ... />` in RN `<Modal>`; reminder/repeat selection now uses `PickerSheet` (time uses `TimerPickerModal`).
- `components/EditReminderSheet.tsx:939` - options + delete confirmation now use `ActionSheet` (kept error/info alerts as `Alert.alert`).
- `app/history.tsx:245` - clear history confirmation now uses `ActionSheet`.
- `lib/time.ts:212` - `getNextIntervalOccurrence(...)` for stable cadence calculations.
- `lib/notifications.ts:292` - reschedule interval from `scheduledFor` (prevents drift on late delivery / Doze).
- `convex/actions.ts:293` - added interval parsing rules ("every N hours/minutes") + bounds (min 15m, max 168h).
- `convex/schema.ts:15` - added optional `soundRepeatCount` / `soundRepeatMode` fields.
