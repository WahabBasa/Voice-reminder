# Devlog - 2025-12-27/28

## Play Store Build Setup - (21:07 → 18:30 next day)

**Status**: ✅ AAB built successfully

Started work on Phase 11 (Monetization & Release). Goal: get a signed AAB for Google Play Console.

### What we did:

1. **Generated keystore** - `voicereminder.keystore` with alias `voicereminder`, password `voicereminder123`
2. **Created signing plugin** - `plugins/withAndroidSigning.js` to inject release signing config
3. **Added versionCode** - `app.json` now has `versionCode: 1` (required for Play Store)

### Issues hit:

**Notifee + Expo SDK 54 incompatibility** (GitHub issue #1262)
- Error: `Could not find method android()` 
- Fix: Updated `plugins/withNotifeeAndroidMaven.js` with code from the GitHub issue
- Key change: Use `require("expo/config-plugins")` not `require("@expo/config-plugins")`

**react-native-reanimated requires New Architecture**
- Can't disable New Arch because reanimated 4.x needs it
- Left `newArchEnabled: true` in app.json

**Windows file locking / EPERM errors**
- Metro bundler crashed with `kill EPERM` after writing bundle
- Fixed by: just retrying, sometimes transient

### Build commands:

```powershell
# Prebuild (regenerates android folder)
npx.cmd expo prebuild --platform android --clean

# Build AAB
cd android
.\gradlew.bat bundleRelease
```

### Output:

```
C:\Dev\VR\android\app\build\outputs\bundle\release\app-release.aab
Size: ~82 MB
```

### Files touched:
- `plugins/withNotifeeAndroidMaven.js` - Fixed for Expo SDK 54
- `plugins/withAndroidSigning.js` - New file, injects signing config
- `app.json` - Added `versionCode: 1`
- `voicereminder.keystore` - New file (not in git, keep safe!)
