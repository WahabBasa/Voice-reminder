# Devlog - 2026-08-12

## Wayfinder launch map + five submission blockers fixed - (00:30-02:45)

**Status**: ✅ map live · ✅ 5 blockers fixed, 691 tests green · 🔒 all publishes held

Night shift. User reported the Notification settings row did nothing on device — turns out
notifee's `openNotificationSettings` is Android-only and the row we kept was the broken one;
the deleted "App settings" row had been the only working iOS path. `openNotificationSettingsSafe()`
(lib/notifications.ts:953) now calls `Linking.openSettings()` unconditionally on iOS, returns a
bool, and diagnostics alerts instead of failing silently.

Then the big one: ran /wayfinder (found on disk, wasn't in the session skill registry) and
charted **OLD-63 "Launch map: VoiceReminder live on the App Store"** — destination locked with
the user, tickets as sub-issues with native Linear blocking, fog kept on the map body. OLD-62
reparented under it.

Two research tickets resolved the same night by Opus agents:

- **App Store rejection** (docs/appstore-rejection-research.md): 8 hard blockers — placeholder
  RC iOS key, no 5.1.2(i) AI-consent surface, privacy policy unhosted AND factually wrong,
  paywall missing Restore/disclosures/links, literal "Secure payment via Google Play" on iOS,
  empty nutrition labels. Myth killed: `com.apple.developer.alarmkit` entitlement doesn't exist.
- **AlarmKit banner** (docs/alarm-banner-research.md): locked alert is a fixed system template —
  we control title, tint, one secondary button, nothing else (stopButton deprecated in 26.1).
  Real canvas = Live Activity widget (countdown/paused only) or foreground stop-and-takeover,
  which needs the same native machinery as the CL-4 pass.

Graduated 7 tickets (OLD-74..80), then a 5-agent Opus fleet + verify agent fixed the workable
five in one pass (tsc clean, 691/691):

- **Reminders leak (OLD-74, urgent)**: `convex/reminders.ts` returned every user's reminders
  unauthenticated. New `lib/deviceId.ts` (128-bit install id), `deviceId` required on every
  network-reachable reminder endpoint, ownership checks, 9 client call sites threaded, all other
  endpoints audited. NOTE: breaking both directions — **convex deploy + OTA must ship together**.
- **AI consent (OLD-75)**: sheet gates the first recording, revocable via new Settings Privacy row.
- **Paywall (OLD-76)**: Restore flows, Schedule-2 disclosure from live offering data, Terms/Privacy
  links, honest benefits, categorized purchase errors.
- **Sentry (OLD-77)**: release builds stop streaming console (incl. a second leak via default
  breadcrumbs integration the research missed).
- **AlarmKit basics (OLD-78)**: literal #3D8BFF tint + 26.1 init with 26.0 fallback (needs rebuild).

Everything held from publishing on user request ("things to go over before publishing").
