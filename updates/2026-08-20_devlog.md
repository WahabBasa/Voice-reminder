# Devlog - 2026-08-20

## App Store review: 2.1 info request, reply pack built - (morning)

**Status**: ⚠️ Apple wants an info pack before reviewing

Build 2 got a Guideline 2.1 "Information Needed" — mildest rejection type, zero
findings. Full reply drafted in `docs/asc-review-notes.md`: items 2-7 final text,
item 1 waits on a screen-recording link. No accounts in the app = no demo
credentials needed. Provider names (OpenAI/Speechify/ElevenLabs/RevenueCat/Sentry/
Convex) disclosed — reviewer-facing notes, not app copy, so the no-provider-names
rule doesn't apply.

## Killed the "Checking your plan..." gate - (afternoon)

**Status**: ✅ 948 tests green, tsc clean

Mic button and typed composer both blocked on a network RevenueCat call at tap time.
Rewrote to cached entitlement (Opus agent, two passes):

- `lib/purchases.ts:130-149` — new `refreshProStatus()`: forced re-read, fails safe
  to free
- `app/index.tsx` — both gates read cache + local count synchronously; at-cap free
  users lock instantly with the upgrade CTA; background refresh can only unlock
- `components/RecordingOverlay.tsx:362-366` — fallback copy → "Getting ready..."
- `lib/store.ts:320` creation-time check untouched as the backstop

Known gotcha: `npm.cmd run test:coverage` exits 1 on this Windows box even on a
clean checkout — jest coverageThreshold path-separator artifact. Verified against
stashed baseline; Linux CI unaffected.

## Review-demo video: recorded, then agent-edited - (evening)

**Status**: ✅ edited mp4 ready · ⚠️ source has no mic audio

5:30 screen recording edited by a 3-agent pipeline (eyes coarse → eyes fine →
hands): purchase section 3x + email blurred across 10 windows (incl. sliding
sheets), 2.5-min dead wait 8x, +10.5 dB gain (+14 would have clipped), purchase
chime carved out at 1x. All 8 verification checks passed; output
`Downloads/remi-review-demo.mp4` (2:35). Source recorded with the screen-recorder
mic OFF — the voice-creation flow is silent. Flagged for re-record.
