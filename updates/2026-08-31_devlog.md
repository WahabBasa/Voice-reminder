# Devlog - 2026-08-31

## Build 2 verified on device, subscription card built + shipped - (07:35-10:30)

**Status**: ✅ OLD-117..120 confirmed on the phone; new Pro card live via OTA dd886493

User checked TestFlight build 2 after the force-quit-twice dance: red Overdue group with
the five stale one-offs showed up on Today. "The overdue works." That closes the loop on
Friday's ship.

Then: "App doesn't show if am subscribed or not." RevenueCat said the device *was* Pro
(active entitlement, sandbox expiry 13:23 local) — the app just had no UI reading it. The
Settings "Upgrade to Pro" card rendered unconditionally. Opus agent built the fix:

- `app/settings.tsx` — card branches on entitlement: Pro → "Remi Pro / Active · Unlimited
  reminders", tap opens native manage-subscriptions; free → unchanged card → paywall.
  Seeds from `getCachedProStatus()` (no flash), re-checks on `useFocusEffect`.
- `lib/purchases.ts:152-190` — `openManageSubscriptions()` with store-URL fallback. The
  `https://` literal lives here because `legalLinks.test.ts` bans it from settings.tsx.
- `lib/proCardContent.ts` (new) + 9 tests at 100%; threshold added to `jest.config.js`.

tsc clean, 34 suites / 1033 tests. Published: `eas update` group
`dd886493-7d89-4feb-9d35-a6d92715b608`, branch production, runtime 1.0.0. Convex untouched.

Two gotchas found by the publish agent, now in CLAUDE.md: `cd /d C:\Dev\VR` is a no-op when
cmd inherits the lowercase path (bounce via `C:\Windows` first or Metro throws the SHA-1
error), and `test:coverage` **always** exits 1 on Windows — threshold keys never match the
coverage map paths. Trust the printed results locally; Linux CI is the real gate.

## Codex partnership: plugin installed, protocol grilled - (09:00-12:45)

**Status**: ✅ Working end-to-end; needs a session restart to bind /codex:* commands

Four Opus researchers mapped the integration surface. Short version: official
`openai/codex-plugin-cc` plugin is the blessed Claude→Codex lane (installed, v1.0.6 —
review/adversarial-review/rescue/transfer/status/result/cancel); npm codex turned out to be
*broken* (half-installed, off PATH), rebuilt to codex-cli 0.151.0 and verified against the
user's gpt-5.6-sol config; on Windows there is NO app-server socket (github issue #37450),
so live-socket bridging is out — `codex queue --thread` into the desktop app and headless
`codex exec` are the lanes. Full protocol (grilled with the user, saved to agent memory):
Codex is the iOS blind-spot illuminator — it grills Claude's plans, explores the repo
itself, background rounds on gpt-5.6-sol pinned to HIGH (not xhigh — cost), digest to user,
implementation waits for his "go".

CLAUDE.md also gained a Delegation Policy section: Opus subagents do the grunt work,
orchestrator reviews.

Loose ends spotted: composio API key returns 401 everywhere; `ptc-microsoft-todo` skill
missing frontmatter (Codex complains).

### Next Session

1. Restart Claude Code session → `/codex:*` binds; first real Codex round on whatever plan
   comes up next.
2. OLD-121 backfill — the 08-31 one-off: check whether it rang at the right time today;
   backfill remaining UTC-skewed rows either way. Then OLD-122..125.
3. 2.1 resubmit (re-record demo with mic, reply + resubmit) — still the long pole to launch.
4. OLD-126 prod switch before public release; OLD-74 spot-check (second account can't read
   your reminders) → Done.
5. Carry: voice latency (OLD-82/106), In Review sweep, Arabic listing, RC "no packages for
   offering default", voice-loop/*.py uncommitted edits (user's, decide), Windows coverage
   threshold path fix, dead composio key.
