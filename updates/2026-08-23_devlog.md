# Devlog - 2026-08-23

## Reviewer detail pack: what build 2 actually compiled - (evening)

**Status**: ✅ 5 of 8 questions answered from artifacts; device run prepped

External reviewer asked for the AlarmKit lock-screen intent details (device, build,
behavior matrix, logs, compiled source, toolchain, perform() timing, supportedModes).
Everything answerable without the phone got answered from the EAS artifacts:

- Build 2 = commit `bf20960`, EAS `4732b871`, Xcode 26.0 (17A324), iPhoneOS 26.0 SDK
  (23A339), Swift mode 5. Xcode log is brotli — `curl --compressed`.
- The IPA's `Metadata.appintents/extract.actionsdata` shows both intents compiled with
  `openAppWhenRun: false`, `supportedModes: 1`, `authenticationPolicy: 0` (implicit
  default = alwaysAllowed, runs while locked).
- Decoded `supportedModes: 1` == `[.background]` empirically: the 08-15 dev build
  (VRStopIntent still openAppWhenRun=true) compiled to `2`, same toolchain. So the
  reviewer's proposed fix — declaring `supportedModes = [.background]` — is a no-op:
  build 2 already shipped background-only. Apple never publishes the raw values;
  research agent confirmed the defaults story from docs + WWDC25 244/230.
- `withAlarmKit.js` copies `VRAlarmIntents.swift` verbatim and the file is identical
  bf20960..HEAD → plugin source == compiled source.

Gap found: neither intent had a single `os_log`, so device logs could never prove
perform() ran. Instrumented both (`Logger`, subsystem com.wahabbasa.VoiceReminder,
category VRAlarmIntents, `.public`) + added the explicit `supportedModes` declaration.
Guard-2's exact `guard … else { return .result() }` shape is pinned by
`alarmIntentsSource.test.ts` — skip-log had to go before the guard. 159 tests green.

- `plugins/ios-src/VRAlarmIntents.swift` — perform start/end, stopRinging outcome,
  guard-2 skip logs; supportedModes on both intents

### Next Session

1. EAS `development` build with the instrumentation, install via link (store builds
   have no Install button — that was the "IPA download" confusion).
2. Device matrix: Later/Done × backgrounded/force-quit/fresh/locked, Metro streaming.
3. Windows syslog route if needed: `pymobiledevice3 syslog live` (no Mac).
4. Carry: 2.1 resubmit recording, OLD-116, shot-1 reshoot, Arabic listing, RC cleanup.
