# Devlog - 2026-08-14

## Catches live on dev, legal site hosted, consent sheet shrunk - (11:00-13:30)

**Status**: ✅ catches live (dev) · ✅ legal pages live · ✅ consent card built · 🔨 native rebuild pending

- **Catch feature pushed to dev** (`convex dev --once`, adds `speechCatchState.by_device`). Backend-only,
  so no OTA: new reminders now speak "Heads up — ..." / "يا وهاب، ..." with rotation. Existing
  reminders keep their old audio.
- **gh CLI installed for real autonomy.** No winget in any shell PATH, so: direct zip →
  `C:\Users\AtheA\Tools\gh-cli`, added to user PATH, user did `gh auth login` (browser flow, token in
  credential manager — never in chat). Created repo + enabled Pages via API without touching the browser.
- **Legal site live**: `legal-site/` → new repo `WahabBasa/voicereminder-legal` → GitHub Pages at
  wahabbasa.github.io/voicereminder-legal (live ~50s after enable). Privacy ~1.4k words, terms ~1.35k,
  Tiimo-modeled structure/tone, dark mode, zero vendor names (grep-verified), medical-device callout,
  Apple standard EULA incorporated. Every factual claim checked against code before writing (real finds:
  release Sentry is crash-only; deleting a reminder deletes server audio). Old factually-wrong
  `docs/privacy/index.html` deleted so it can't get hosted by accident. Support email switched to
  athea.hab@gmail.com. After user feedback ("sounds like giving user data to other AI companies"),
  reframed sections 3/5/7 Tiimo-style: "We do not sell your personal data", pipeline described as steps
  not hand-offs, providers = machinery on our behalf under contract. In-app consent card keeps
  "third-party" — that's the word 5.1.2(i) consent exists to say.
- **Consent sheet → compact card** (`components/AiConsentCard.tsx`): two sentences + Allow / Not now,
  Allow persists consent then chains straight into the iOS mic prompt. Same consent semantics + Settings
  revoke. Settings got About rows (Privacy Policy / Terms of Use), paywall links re-pointed — all URLs
  from `lib/legalLinks.ts`. Stale 404 URLs in `lib/purchases.ts` removed. 829/829, tsc clean.
- ⚠️ `expo-web-browser` is a NATIVE module (auto-added to app.json plugins): the legal-link taps and
  consent-card "Learn more" need the next native build — OTA alone would crash them. Pairs with the
  OLD-78 tint fix already waiting on a rebuild.
- CL-4 question from user (interrupt first alarm, re-ring with new sound): walked the research verdict —
  foreground stop-and-replace is buildable (native), unattended is an Apple wall; the OLD-64 locked-phone
  overlap test remains the decider.

### Next Session
1. Native rebuild (`expo prebuild` — app.json changed — + assembleRelease): picks up expo-web-browser +
   OLD-78 tint. Then device pass: catch audio on a new reminder, consent card flow, legal links.
2. OLD-64 locked-phone overlap test (ship gate; procedure in docs/cl4-escalation-research.md).
3. OLD-82 perf device run: 3 reminders → `[VR PERF SUMMARY]` / `stopRecording_split` / `convex_perf`.
4. RevenueCat via MCP: App Store app, real `appl_` key, offering dedupe; ASC manual side (IAP products,
   Paid Applications Agreement, banking/tax).
5. ASC paperwork: privacy URL into the listing, nutrition labels (map straight from policy §2: Audio,
   Other User Content, Crash/Diagnostics, Purchase History, Device ID), age rating (B7).
6. Parked decisions: catch in dense alarm wav?, wellness-lecture eval lint, name onboarding ("what
   should I call you?" — addressTerm feeds name catches), B5 mic purpose string, A5 "Google Play" string
   on iOS paywall, OLD-66/67 Speechify, OLD-80 Live Activity, OLD-71 icon.

## Continued: voice loop — charted, researched, and talking - (20:24)

**Status**: ✅ working skeleton (typed in, Beatrice out) · ✅ map fully charted, 7 decisions · 🔨 assembly next

Side project, not VR the app: hands-free Claude on the PC. Wake word → Wispr Flow → dedicated Claude
session → Speechify streaming TTS. Charted as wayfinder map OLD-83, then worked most of the frontier in
one evening — all heavy lifting by Opus-high subagents/workflows, main session on coordination only.

- **Research (4 parallel agents, all verdicts in `docs/voice-loop/`)**: openWakeWord by forfeit
  (Porcupine free tier died 2026-06-30); Wispr has an undocumented `wispr-flow://start-hands-free` URL
  scheme (verified live) with transcript-as-one-paste and completion in `main.log`; Python Agent SDK
  `ClaudeSDKClient` + partial messages (scrub `CLAUDECODE` env or nested launch dies); sounddevice on
  WASAPI with `auto_convert=True` (MME default costs ~110 ms and rejects 24 kHz).
- **Speechify key** into PTC keyring (key hit chat — rotate at platform.speechify.ai when convenient).
  Live TTFB: 634–1337 ms, not the advertised 300; delivery 3–5x realtime; `docs/voice-loop/beatrice-sample.wav`.
- **Skeleton built** by a 4-slice Opus workflow (`voice-loop/speak.py`, `brain.py`, `skeleton.py`).
  Measured: input → first audible 3.4–8.0 s, of which model TTFT is 2.7–7.3 s; audio stack ~0.7 s, zero
  inserted silence. Free-tier Speechify = 1 concurrent request → serial FIFO fetcher (loses nothing).
  Run: `VL_INTERACTIVE=1 ptc run -i C:/Dev/VR/voice-loop/skeleton.py` (forward slashes in Git Bash).
- **Perceived-speed grilling (OLD-91)**: design the rhythm, not the speed. Pre-synth micro-ack clips at
  VAD-close → silence → escalation clip @~5 s → failure line @~20 s; no ambient loop; lead-in-sentence
  rule added to the voice prompt (halves first-sentence close). Model itself untouched by request.
- Map state: 7 decisions indexed, fog empty. Open: OLD-92 (assembly — wake listener, capture window,
  ack clips, autostart; Colab training run needs user's Google login) → blocks OLD-89 (workday phrase
  test: does bare "claude" survive vs `hey_claude`/`hey_jarvis`).

### Next Session
1. VR app: native rebuild + device pass, OLD-64 overlap test, OLD-82 perf run, RevenueCat/ASC — all
   still pending from the midday list above (untouched tonight).
2. Voice loop: orchestrate OLD-92 assembly (sliced Opus workflow, same pattern as the skeleton).
3. Voice loop: OLD-89 workday wake-word test once assembly lands.

## Continued: multi-takes, animation, four OTAs, consent row gone, legal reword - (22:51)

**Status**: ✅ all on dev (convex push + OTAs) · ⚠️ device pass pending · 887/887, tsc clean

- **Multi-reminder takes (OLD-93)**. Probed the live model first (`__evals__/multi-reminder.probe.ts`):
  the old prompt returns a bare JSON array for multi-task takes 6/6 — pipeline would break on
  `.title` — and an appended array instruction gets `{"reminders":[...]}` clean 11/12 (EN/AR, mixed
  frequencies). Opus agents then built it: `MULTI_REMINDER_INSTRUCTION` inside `buildSystemPrompt`,
  tolerant `normalizeParsedReminders` in helpers (array / `{reminders}` / bare object, cap 5), both
  action paths loop per item and return `{...first, reminders, reminderCount}`. Slow path lost its
  drifted private prompt copy. Client: new `lib/voiceTake.ts` seam + `app/index.tsx` loops store/
  schedule/hydrate per item with per-item failure isolation. Free-tier gate at store time: overflow
  rows deleted server-side (`api.reminders.remove`), "2 of 3 reminders created" toast → paywall.
  N>1 takes stay on the list (edit sheet would hide siblings). Known quirk, documented with a test:
  `coerceFrequency` reads the whole transcript, so "weekdays" in one item drags its siblings.
- **OLD-94**: `components/ProcessingWave.tsx` (reanimated worklets, no new deps) replaces the
  ActivityIndicator in RecordingOverlay's processing state. Taste test on device pending.
- **EAS publish from agent sessions WORKS.** `powershell -NoProfile` spawn with uppercase
  `Set-Location C:\Dev\VR` + `CI=1` + `--clear-cache`. The 08-13 "only real terminals" conclusion
  was wrong; memory updated with the recipe. Four OTAs went out tonight this way.
- **Red screen after the first OTA**: top-level `expo-web-browser` imports (paywall, settings,
  AiConsentCard) throw on builds without the native module → route undefined → SceneView "Cannot
  read property 'ErrorBoundary' of undefined". Note: import time, not tap time — yesterday's "taps
  will crash" caveat undersold it. Fix: lazy `require` in `lib/legalLinks.ts` `openInAppBrowser`
  with `Linking.openURL` fallback; `legalLinks.test.ts` now tripwires any top-level import.
- **Consent row removed from Settings** (user: confusing, wrong place, Tiimo has nothing like it).
  Withdrawal story is now mic-off in iOS Settings; `privacy.html` §3/§4/§10 reworded to match and
  deployed. The 5.1.2(i) consent card in the record flow stays — that one's not optional.
- **Legal pages Tiimo-reworked** (Opus agent read Tiimo's live policy + terms): "providers who help
  us deliver" framing, §5 as six role/purpose bullets, denial-first ordering, terms claims the AI
  as first-party. Terms §7 now bars providers from training too (aligns with privacy §5 — flagged,
  kept). Zero vendor names, grep-verified. Both pages deployed via gh api.

### Next Session
1. Device pass: multi-take (3 cards + toast), ProcessingWave, catches, consent card flow. Legal
   links open Safari (fallback) until the native rebuild lands.
2. Native rebuild (`expo prebuild` + assembleRelease): expo-web-browser + OLD-78 tint.
3. OLD-64 locked-phone overlap test (ship gate); OLD-82 perf device run.
4. RevenueCat via MCP + ASC manual side; listing: privacy URL, nutrition labels, age rating B7.
5. OLD-71 icon (generate candidates for user), fresh iOS screenshots (April Android zip is stale).
6. Needs user sign-off: free-tier truncate-and-delete default, no-edit-sheet-on-multi-takes.
7. Voice loop: OLD-92 assembly → OLD-89 wake-word test. Campaign-transcript analysis after approval.
