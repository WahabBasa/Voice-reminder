# 2026-09-11 Devlog

## Standing rule: device-test builds are `development` builds - (08:00)

**Status**: ✅ Rule in CLAUDE.md + memory; dev build d57a6285 installed

The ring-state fix shipped as a `preview` build (internal release, no dev menu) and the
user rejected it: anything not going to Apple is a development build, full stop. Added
`EXPO_PUBLIC_VR_PERF_LOGS=1` to the `development` profile env in `eas.json` so perf logs
don't need an OTA, wrote the rule into CLAUDE.md ("Which EAS build profile — standing
rule"), and rebuilt with `--profile development`. Published the current tree to the
`development` update branch (01a08e40) so the dev client's launcher can't pick stale JS.

Installed over USB with go-ios after the link held for 20 s — the PC's USB stack still
flaps (dropped mid-install twice; Apple Mobile Device Service from the 09-09 driver
install never started, a reboot is still the likely fix).

## Continued 2026-09-12 — ring-state test 1 - (07:05)

**Status**: ✅ User: "everything runs as it should" — but see the anomaly

Banner X with the app open: user reports the reminder was marked done immediately.
Syslog: `VRStopIntent perform end … recorded=stopped` at 07:05:07 in the app process,
`VRAlarmHint.post` in the code path, but no `reason=native` reconcile fired and the
07:05:47 tick logged `events=0 applied=0` (the 07:05:17 tick is missing — JS quiet while
the alert was up). Event formats, keys, store container, kind strings and ObjC exports
all match on inspection. Unresolved: which path completed the card, and why the queued
Stop event was invisible to `peekEvents`. Next step is a relaunch to read the launch
reconcile's event count, then a second Stop with the log watched live.

### Next Session
1. Ring-state anomaly above, then the rest of the device matrix (Later real time, 4×
   Later, ignored → Missed, kill mid-ring, two same-day occurrences).
2. Feedback header icon + one-time tip (parked, Codex-agreed).
3. Engine A/B (dictation vs transcriber), Arabic on-device, parse latency.
4. 2.1 resubmission: add `ascAppId` to `eas.json` submit profile, `eas submit` build 4,
   clear sandbox purchase history, record per `docs/asc-review-notes.md:96-121`, update
   ASC privacy labels for the feedback feature.
5. Reboot the PC before the next USB session (AMDS pending since 09-09).
