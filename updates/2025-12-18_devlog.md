# Devlog - 2025-12-18

## Startup - (08:38)

**Status**: 🟡 In progress

- Checked date/time
- Reviewed plan.md (Phases 1-5 ✅, Phase 6 pending)

## Plug-and-play setup fixes - (09:03)

**Status**: ✅ Ready to commit

Made the repo easier to clone + run on a new machine:

- Added Node version pin: `.nvmrc` (22.5.1)
- Added Convex config file: `convex.json`
- Added a simple setup guide: `README.md`
- Added Expo config plugin to auto-add Notifee Maven repo during prebuild:
  - `plugins/withNotifeeAndroidMaven.js`
  - `app.json` - added plugin entry

Files:
- `.nvmrc`
- `convex.json`
- `README.md`
- `plugins/withNotifeeAndroidMaven.js`
- `app.json`

## Local machine setup notes (DELL Win10) - (09:35)

**Status**: ✅ Setup done (will run app on other machine)

Pulled latest `main` and wrote down what this machine is missing so we don't waste time trying to run Android here.

- Node: installed NVM for Windows, then installed/activated Node `22.5.1` (matches `.nvmrc`)
- PowerShell: `npm` / `npx` `.ps1` shims are blocked (scripts disabled) so use `npm.cmd` / `npx.cmd` (or run from `cmd.exe`)
- Env: created `Voice-reminder/.env.local` with Convex URL + deployment
- Android toolchain: not installed here yet (no Android Studio/SDK/adb; Java is still 8)
- Minor git hiccup: I created an untracked `updates/2025-12-18_devlog.md` before pulling; remote has the same file now, so I moved my local one to `updates/2025-12-18_devlog.local.md`

## Icon swap + reduced blue usage - (19:57)

**Status**: ✅ Working (tsc passes)

Main goal was “less blue everywhere” and more premium, consistent icons.

- Swapped icon implementation from `@expo/vector-icons` (Feather) to `lucide-react-native` via a single wrapper component so we have one place to control defaults (size, stroke width, default color).
- Kept brand blue for true “primary” UI (selected pills, primary CTA buttons, FAB), but moved most decorative/list icons to neutral greys to avoid the “AI light-blue everywhere” look.
- Normalized some icon badge backgrounds from `colors.accentLight` → `colors.muted` so the blue doesn’t show up in every row/card.

Files:
- `components/AppIcon.tsx:89` - new wrapper + mapping for all icon names we use (`bell`, `mic`, `clock`, `trash-2`, etc.)
- `app/index.tsx:29` - replaced card icons + settings icon + FAB icon
- `app/settings.tsx:8` - replaced row icons; made row icon container background neutral (`colors.muted`)
- `app/history.tsx:16` - replaced status icons + back/clear icons; made row icon container background neutral (`colors.muted`)
- `app/reminder/new.tsx:23` - replaced header/option/time/info icons; kept blue only for selected states
- `app/reminder/edit.tsx:30` - replaced header/option/time/preview/volume icons
- `components/ReminderCard.tsx:10` - replaced card icons; neutralized icon badge background
- `components/RecordingOverlay.tsx:15` - replaced recording controls icons; kept pro “zap” as brand accent
- `components/DetailSheet.tsx:17` - replaced option/time/preview/volume/delete icons
- `package.json:30` - added `lucide-react-native`
- `package.json:38` - added `react-native-svg`

Notes:
- `expo-doctor` still complains about `app.json` having `android.navigationBarColor` and some Expo patch-version mismatches; unrelated to the icon work.
