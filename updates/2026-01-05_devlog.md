# Devlog - 2026-01-05

## Session Start - (00:10)

**Status**: 🟢 Ready to develop

Starting fresh session. Metro running on port 8084. Yesterday's focus was on volume control debugging.

---

## Subscription Limit Enforcement (00:13 - 00:25)

**Goal**: Enforce 5-reminder limit for free tier

**Findings**:
- `checkCanCreateReminder()` existed but was never called
- Users could create unlimited reminders

**Changes**:
- `lib/usage.ts` - Now counts actual reminders from storage (no separate counter)
- `lib/storage.ts` - `addReminder()` checks limit before creation, throws `ReminderLimitExceededError`
- `app/index.tsx` - Pre-check on FAB tap, redirects to paywall if limit reached
- `app/reminder/new.tsx` - Same pre-check before API call

**Result**: Limit enforced BEFORE recording starts = no wasted API credits

---

## Backend Error Handling (00:35 - 00:45)

**Goal**: Stop surfacing SDK errors to users

**Changes**:
- `lib/purchases.ts` - All RevenueCat errors now use `console.log` (silent)
- `app/paywall.tsx` - Replaced ugly toast with inline error banner (styled like offline message)

**Error Banner Features**:
- Appears above footer
- Auto-dismisses after 4 seconds
- Dismissable via X button

---

## Character Limits for TTS (00:43 - 00:45)

**Goal**: Prevent excessive API usage while keeping UX comfortable

**Limits Added**:
| Field | Max Length |
|-------|------------|
| Title | 100 chars |
| Sound text / Description | 250 chars |

**Files Updated**: `EditReminderSheet.tsx`, `app/reminder/new.tsx`

---

## Session 2 - Release Build & Bug Fixes (05:30 - 07:45)

### ARM-Only Build Optimization - (05:35)

**Status**: ✅ Done

Created plugin to only build for ARM architectures (real devices), skipping x86/x86_64 (emulators).

**Result**: Build time dropped from ~37 min to ~3 min. AAB size from ~82 MB to ~50 MB.

**Files**:
- `plugins/withArmOnlyBuild.js` - NEW, modifies `gradle.properties` during prebuild
- `app.json` - Added plugin to plugins array

---

### Version Code Bump & AAB Build - (05:44)

**Status**: ✅ Build 6 ready for closed testing

- Bumped `versionCode` from 5 to 6 in `app.json`
- Built AAB: `android/app/build/outputs/bundle/release/app-release.aab`

---

### ElevenLabs Voice ID Update - (07:36)

Changed to new voice ID `19STyYD15bswVz51nqLf` via:
```bash
npx convex env set ELEVENLABS_VOICE_ID 19STyYD15bswVz51nqLf
```

---

### Play Button Stuck Bug Fix - (07:43)

**Status**: ✅ Fixed

**Bug**: After alarm fired during voice regeneration, the play button got stuck in "Stop" state.

**Root Cause**: `isPlaying` state and `sound` object got out of sync when audio URL changed.

**Fix** (`EditReminderSheet.tsx`):
1. Reset `isPlaying` in error handler - button no longer stuck on playback failure
2. Clear stale sound after regeneration - unload old sound before setting new URL
3. Added effect to detect URL changes - auto-clears sound when `reminder.audioUrl` changes

---
