# Devlog - 2026-09-19

## Build 6 rejected (2.1(b) + 3.1.2(c)), resubmitted with build 7 - (12:30)

**Status**: ✅ New submission in, Waiting for Review — app version + Pro group + both subs in ONE submission

Apple came back 03:21 on submission 373e825f, reviewed on iPhone 17 Pro Max + iPad Air M3. Two issues, neither about app code:

- **2.1(b)**: the subscriptions were never submitted for review. First sub group has to ride with an app version and last time only the version went.
- **3.1.2(c)**: no Terms of Use (EULA) link in the App Store metadata. The app had it, the listing didn't.

What got done in ASC (user clicking, me guiding):

- Both sub review screenshots were the old "Plans couldn't load" paywall. Swapped for a clean iPhone capture with prices, added a review note on each (`vr_pro_annual`, `vr_pro_monthly`). Screenshot uploads autosave — Save stays grey, that's normal.
- Terms + Privacy links appended to the App Description. First attempt didn't save (counter still said 3,102) — check the counter, it should read 2,950.
- Build 7 attached, condensed resubmission block on top of App Review Notes, trimmed recording in the Attachment field (`LANDrop\remi-paywall-build7.mp4`, 51 s, first 22 s of TestFlight install cut with ffmpeg). Attachment shows "Waiting for…" until you Save.

The part that cost time: the version sat in Wednesday's "Unresolved Issues" submission, the subs went into a new draft, and the draft can't submit without an app version. Add for Review never offers the old submission as a target, and the old version-page "In-App Purchases and Subscriptions" section is gone in the current UI. What worked: old submission → red minus on the iOS App row → Remove ("You'll need to resubmit this item in a different submission") → version page → Add for Review → Draft Submission → 4 items, Submit goes blue. The message thread stayed visible on the removed submission.

Code, shipped in build 7:

- `components/paywall/FooterLegalRow.tsx` - new, `Terms of Use · Privacy Policy · Restore` in the sticky footer so it's visible without scrolling (user's call — reviewer's screenshot stopped above the ClosingBlock links). Underlined, one ink, flexWrap so big Dynamic Type wraps instead of clipping.
- `app/paywall.tsx:359-368` - row under the caption lines, reuses handleOpenLink / handleRestore
- `__tests__/components/footerLegalRow.test.tsx` - 6 tests
- `app.json:14` - buildNumber 7 (autoIncrement)

Build `06507b1e`, EAS submission `378487f7`. Built from an uncommitted tree; committed after. Agent dispatch of `eas build` got classifier-denied as a production deploy — ran it bare from Bash once the user said "run the build".

Not a bug: Pro "activating itself" after reinstall is the 09-15 sandbox purchase still live on the Apple ID (TestFlight month ≈ 1 day). Restore only runs on button taps (`app/paywall.tsx:210`). While Pro there's no route to the paywall, so cancel via Settings → Pro card and wait out the period before recording. The recording ends with another sandbox Annual purchase, so the phone is Pro again for a day or so.

### Next Session
1. Watch the verdict on the new submission. If approved: OLD-126 Convex prod switch before any release (version is on Manual release). No production OTA and no Convex push while build 7 is in review — both reach the reviewer.
2. One-minute reminders ring with the default sound: alarm registers with the fallback, voice swap (`lib/notifications.ts:1808` refreshAlarmKitSound) lands too late or never runs because the phone got locked. Measure first on a dev build with the syslog rolling — app left open vs locked straight away.
3. Feedback needs a founder UI + a way for Claude to reply from chat (today it's `convex run` by hand).
4. Ring-state anomaly from 09-12 07:05 (banner X marked done, `events=0`), then the rest of the device matrix.
5. Feedback header icon + one-time tip (parked, Codex-agreed).
6. Engine A/B (dictation vs transcriber), Arabic on-device, parse latency.
7. Codex: MCP server with key `ak_…mXj4` returns 401 on every start (config.toml) — fix or drop.
