# Devlog - 2026-08-16

## CI mystery solved, three same-day fixes shipped, ship gate closed, ASC paperwork done - (06:56-13:40)

**Status**: ✅ OLD-64 passed & closed · ✅ OTA 1814e797 live · ✅ 975/975 · ✅ ASC privacy + age rating published

- CI "always fails on GitHub" mystery: every red run (5-8, Aug 11-14) died on `npm run test:coverage` -
  per-file thresholds that plain `npm test` never checks locally. Fixed the habit, not the infra:
  CLAUDE.md gates every push on test:coverage, global wrap-up skill got a pre-push check step, CI
  drops the redundant plain `npm test` step (suite ran twice per push). 4336778.
- Luna swap decided: keep. Committed ae0aadf + convex dev push. Parse runs gpt-5.6-luna now.
- Device pass on cf4a5b0e: locked-phone nag chain held unattended, and the overlap answer is
  REPLACE, not mix. User findings mid-test became three issues, all fixed by Opus agents in-session
  (b61a8bc):
  - OLD-103: regular reminders' alarm wav was [line][~25s silence] per loop - dense shape was
    persistent-only. Now single shape: [line][2s gap] repeated to 28s. New regression test scans the
    wav body for silence runs longer than one gap.
  - OLD-104: phrasing canon collapsed to two shapes - actions bare imperative ("Drink your water."),
    events "[X] is right now.", EN+AR. Old canon pinned dead by test; evals grew POLITENESS +
    padded-action lints.
  - OLD-105: one device-aware clock formatter (lib/time.ts); Today/Days/edit sheet/chips show am/pm
    on 12h devices, padded 24h otherwise.
- Two convex dev pushes (11:46, 11:59) + OTA to development, group 1814e797. User verified by ear:
  works as expected. OLD-64 closed with written results. Stored wavs are immutable - only fresh
  reminders carry the new audio/lines.
- ASC live walkthrough: privacy labels published (Audio, Other User Content, Device ID, Purchases,
  Crash Data - all App Functionality, linked except crash data, tracking No), age rating 4+ (172
  regions), subtitle "The talking alarm clock", App Store server notifications -> RevenueCat URL
  (prod + sandbox). Shared secret skipped - RC runs on the .p8 subscription key.
- app.json: supportsTablet false, privacyManifests mirroring the published labels (encryption
  exemption was already pinned). Native config - takes effect at the production build, not OTA.
- .p8 IAP key moved from Downloads\LANDrop to repo root (gitignored via *.p8).

### Next Session
1. Screenshots + listing copy (OLD-63) - medical_theme-screenshots.zip is the reference.
2. Remi rebrand pass: display name, PRO_PRODUCT_NAME, Android adaptive icon stale pre-rebrand.
3. Production build + eas submit - app.json native changes ride it; submit subscriptions WITH the
   version (first IAP review).
4. In Review sweep pending explicit verification: OLD-95..102, 74-78, 58-61, 93, 94 (say the word
   to sweep what the sign-off covers).
5. Carry: stop-button semantics, live evals, residual cleanup (alarm.tsx debug route, DatePickerModal
   dead half, typedTake coverage, drop the unused `persistent` validator arg).
