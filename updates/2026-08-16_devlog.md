# Devlog - 2026-08-16

## CI mystery solved, three same-day fixes shipped, ship gate closed, ASC paperwork done - (06:56-13:40)

**Status**: ✅ OLD-64 passed & closed · ✅ OTA 1814e797 live · ✅ 975/975 · ✅ ASC privacy + age rating published

- CI "always fails on GitHub" mystery: every red run (5-8, Aug 11-14) died on `npm run test:coverage` -
  per-file thresholds that plain `npm test` never checks locally. Fixed the habit, not the infra:
  CLAUDE.md gates every push on test:coverage, global wrap-up skill got a pre-push check step, CI
  drops the redundant plain `npm test` step (suite ran twice per push). 4336778.
- Luna swap decided: keep. Committed ae0aadf + convex dev push. Parse runs gpt-5.6-luna now.
- Device pass on cf4a5b0e: locked-phone nag chain held unattended, and the overlap answer is
  REPLACE, not mix. User findings mid-test became three issues, all fixed by Opus agents in-session
  (b61a8bc):
  - OLD-103: regular reminders' alarm wav was [line][~25s silence] per loop - dense shape was
    persistent-only. Now single shape: [line][2s gap] repeated to 28s. New regression test scans the
    wav body for silence runs longer than one gap.
  - OLD-104: phrasing canon collapsed to two shapes - actions bare imperative ("Drink your water."),
    events "[X] is right now.", EN+AR. Old canon pinned dead by test; evals grew POLITENESS +
    padded-action lints.
  - OLD-105: one device-aware clock formatter (lib/time.ts); Today/Days/edit sheet/chips show am/pm
    on 12h devices, padded 24h otherwise.
- Two convex dev pushes (11:46, 11:59) + OTA to development, group 1814e797. User verified by ear:
  works as expected. OLD-64 closed with written results. Stored wavs are immutable - only fresh
  reminders carry the new audio/lines.
- ASC live walkthrough: privacy labels published (Audio, Other User Content, Device ID, Purchases,
  Crash Data - all App Functionality, linked except crash data, tracking No), age rating 4+ (172
  regions), subtitle "The talking alarm clock", App Store server notifications -> RevenueCat URL
  (prod + sandbox). Shared secret skipped - RC runs on the .p8 subscription key.
- app.json: supportsTablet false, privacyManifests mirroring the published labels (encryption
  exemption was already pinned). Native config - takes effect at the production build, not OTA.
- .p8 IAP key moved from Downloads\LANDrop to repo root (gitignored via *.p8).

## ASO playbook, listing copy, ASC pass, latency diagnosis - (17:35-23:30)

**Status**: ✅ Listing text locked & pasted into ASC · ✅ 6 demo reminders seeded · OLD-106/107 filed

- Distilled the two founder transcripts (ASA podcast + AppSprint ASO video) into
  docs/aso-listing-playbook.md. Title `Remi: Voice Reminders` (the competitor reference app is
  literally named "Voice Reminder"), subtitle keeps "The talking alarm clock", 93-char keyword
  field, no keyword repeated across fields (Apple ranks the weaker placement). `adhd` hidden-field only.
- Copy register locked from competitor reviews - direct statements in reviewers' own words. Promo:
  `"Drink your water." An alarm that says your reminder out loud - on time, and again until it's done.`
- ASC live walkthrough: version-page text in, Pricing Free/all countries, subs both moved to Level 1
  (annual was Level 2 = downgrade trap), group localization "Remi Pro", review screenshots deferred
  to post-rebrand. Regulated Medical Devices: skipped, doesn't apply.
- Seeded 6 demo reminders through the real pipeline for screenshots; removed 3 stale ones (incl. the
  Suno card - personal data + brand name can't be in store shots).
- "Way too long" reminder creation → profiler agent: parse is 92% of the 1.2-1.5s action; 15k-char
  prompt uncacheable (clock at char 4,486, SPOKEN_LINE_RULE inlined 3x); one 18s outlier; silent
  base64 fallback blocks the card on the full TTS ladder. Target relaxed to 3s → OLD-106 + OLD-107.

## Latency landed, ladder stripped, Beatrice everywhere - (Aug 17, 00:30-11:30)

**Status**: ✅ OLD-106/107/108/62 implemented + deployed to dev · ✅ OTA b3d66741 · ✅ 935/935 · OLD-66 Done

- OLD-106 (agent): prompt 14.9k→12.2k chars, stable cacheable prefix 30%→99% (clock moved to the
  end), keep-warm cron (cold start NOT confirmed - the 18s was CLI overhead), voice upload-URL
  prefetch, stopRecording split (mode switch guaranteed via play()-choke-point await), fallback
  telemetry `voice_fallback_base64` → Sentry, storage delete off the voice critical path. gptMs
  unchanged within noise - structural win real, latency win not demonstrated. Bonus: fixed a real
  eval bug (OLD-93 envelope unwrap) and repointed the phrasing eval to gpt-5.6-luna.
- OLD-107 (agent): audio-ready 3-4s→1.6s routine, 6-9s→2.1s urgent. ElevenLabs plan caps at 2
  concurrent requests = ONE line, so the win is decoupling extras behind `audioExtrasStatus`
  (two-phase hydration), not parallelism. New 429 retry fixed multi-takes silently losing audio.
- OLD-108: replay variants were escalation-era leftovers; ruling is the nag repeats the same line.
  Agent stalled twice (network), finished inline: net -1,166 lines, opener-diversity eval check
  retired with the behavior it policed. Probe: urgent reminder = one line + one wav, variant fields null.
- OLD-62/66: Speechify switch inline. `containsArabicScript` picks the model per line -
  beatrice_32 on simba-3.2 (EN) / simba-multilingual (AR; user: "just switch", no ear-test).
  ElevenLabs = keyless fallback only. Overnight listen-test pack (26 wavs in listen-test/) +
  findings on OLD-67: pcm_22050 drop-in exact to the ms, SSML pitch/volume/emphasis ignored,
  emotions unvalidated (typos fail silent), Arabic 200-and-mangles on 3.2. Speechify also 429s on
  parallel mp3+pcm - retry absorbs.
- OTA finally shipped via scratchpad .bat (CI=1 + uppercase C: - lowercase cwd was the SHA-1
  killer; two failed attempts first). Update group b3d66741, development branch.
- Security: agent scan found scratch files from the evening holding both live TTS keys in plaintext
  (local temp only, never repo/git) - deleted. Rotating both keys recommended, user's call.
- OLD-109 filed: app-language selection (first-run step + Settings row) feeding TTS routing.

### Next Session
1. Remi rebrand pass: display name, PRO_PRODUCT_NAME, Android adaptive icon - gates screenshots.
2. Screenshots: 5 captures (alarm, lock screen, voice composer, schedule grid, Today) → ChatGPT
   framing at 1290x2796, captions from the playbook → ASC. Plus the subs' paywall review screenshot.
3. Production build + eas submit, subscriptions attached to the version (first IAP review = long pole).
4. Arabic locale listing (title/subtitle/keywords/captions - playbook has the plan).
5. User verification: Beatrice EN+AR by ear on device, creation-speed feel, then the In Review sweep
   (~25 issues incl. 106/107/108/62 - say the word).
6. Carry: stop-button semantics, live evals, alarm.tsx debug route, DatePickerModal dead half,
   typedTake coverage, unused `persistent` validator arg, TTS key rotation, OLD-109.
