# Devlog - 2026-01-26

## Repeat cadence UX + every-N-days - (01:19)

**Status**: OK. Repeat UI stable; alarm not firing

Main issue: the repeat task UI was either doing nothing or crashing when selecting minute/hour/days/weekly repetition. I fixed this by:

- Removing the bottom-sheet picker flow and using numeric input for `Minute/Hour/Days`, plus weekday chips for `Weekly`.
- Adding calendar-based "Every N Days" (stored as `frequency="daily"` + `intervalDays`) and plumbing it through scheduling + rescheduling + next-due UI labels.
- Fixing the bottom-sheet invariant crash caused by `ActionSheet` using invalid snap points (`["auto"]`).

ADB smoke run (Samsung SM_G998U1):
- Opened reminder edit ("Pay for parking") → Repeat modal.
- Minute: `10` shows "Minimum 15 minutes" and blocks DONE; `15` accepted.
- Hour: `200` shows "Maximum 168 hours (7 days)" and blocks DONE; `168` accepted.
- Days: `31` shows "Enter 1-30 days" and blocks DONE; `3` saved and edit sheet shows "Every 3 Days".
- Weekly: "Repeat every" is hidden; DONE blocked until at least one day selected; selecting Monday saved and edit sheet shows "Mon".

Known issue:
- Alarm/notification still not firing at scheduled time (phone just sits there when the reminder time comes). Not tackled this session.

Checks:
- `npx.cmd tsc --noEmit`

Files touched (key spots):
- `components/ActionSheet.tsx:95` - snapPoints fix.
- `components/RepeatTaskModal.tsx:16` - repeat modes; `components/RepeatTaskModal.tsx:78` - min/max validation.
- `components/EditReminderSheet.tsx:373` - map "days" to `daily` + `intervalDays`.
- `lib/store.ts:41` - add `intervalDays` + `scheduledFor`.
- `lib/time.ts:84` - due timestamp respects `intervalDays` + reference time.
- `lib/notifications.ts:139` - include `intervalDays` in schedule + payload + reschedule.
- `app/index.tsx:600` - next-due UI passes `intervalDays` / `scheduledFor`.
- `components/ReminderCard.tsx:39` - pass `intervalDays`; label "Every N Days".

## Alarm scheduling diagnostics + Android 12+ gate - (02:40)

**Status**: OK. Much clearer why alarms don’t fire on some devices

Did a pass on the “alarm not going off” issue. Main changes:

- Stopped trying to auto-open `/alarm` from `EventType.DELIVERED` (Android blocks background UI launches a lot of the time), and kept navigation only on notification press.
- Added cold-start routing: if the app was opened from a notification press, it now routes straight to `/alarm` on launch.
- Added an Android 12+ exact-alarm gate. If `Alarms & reminders` isn’t enabled, scheduling throws `ExactAlarmPermissionError` and the UI nudges you to diagnostics.
- Added an in-app “Alarm diagnostics” screen to inspect `notifee.getNotificationSettings()` + scheduled trigger IDs, with quick links to relevant system settings.
- Added audible fallback by setting channel `sound: "default"` so users notice delivery even if the alarm UI can’t open (channel immutability caveat still applies).

Heads-up / follow-up:
- Need to verify notification press routing when the app is backgrounded (not killed). If Android sends PRESS via background handler in that state, we may need to handle PRESS there too.

Checks:
- `npx.cmd tsc --noEmit`

Files:
- `index.ts:53` - don’t navigate on DELIVERED in background handler
- `app/_layout.tsx:32` - handle `getInitialNotification()` and route to `/alarm`
- `lib/notifications.ts:76` - Android 12+ exact alarm gate + helpers
- `lib/notifications.ts:169` - channel `sound: "default"` fallback
- `app/diagnostics.tsx:22` - new diagnostics screen
- `app/settings.tsx:81` - link to diagnostics
- `app/index.tsx:283` / `app/reminder/new.tsx:118` - surface `ExactAlarmPermissionError` to user
- `components/EditReminderSheet.tsx:341` - regenerate/schedule path handles exact alarm error
